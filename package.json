{
  "name": "sach-viet",
  "private": true,
  "description": "Repo-root stub so Vercel Git (rootDirectory unset) detects Next.js. Real app lives in app/web; vercel.json installCommand materializes it at the build root.",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "next": "16.2.11",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
