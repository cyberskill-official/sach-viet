# Post-merge production smoke — PR #56 (2026-08-24)

**Production URL:** https://sachviet.cyberskill.world  
**PR #56:** merged 2026-08-24 (`84e64b7`) — fix(TASK-UI-005): 18 QA audit bugs  
**Tester:** agent (Playwright MCP + Supabase MCP + curl)

## Part 1 — DB cleanup (BUG-09)

| Field | Value |
|-------|-------|
| **Status** | **Done** — deleted from Supabase `eskazygpnygqsrcwlszz` |
| **Product id** | `b55b1b248e40be084c426f763a10b1bb` |
| **Slug** | `sachviet-day2-demo` |
| **Title** | Sách Việt Day-2 Demo |
| **Variant id** | `d16e5db346cd383b52414c7202cbd176` (SKU `SV-DAY2-DEMO-01`) |
| **Offer id** | `ee13b38b6f52fee5dec2f7df340d53a7` |
| **Dependents removed** | 1 vendor_offer, 1 product_variant, 0 media/wishlist/reviews |
| **Order history** | 3 `order_items` rows retained (1 paid, 2 payment_failed) — no FK to `products` |
| **Post-delete catalog** | 10 public products; API confirms demo absent (~2s cache lag after delete) |

**SQL executed (transaction):**
```sql
DELETE FROM vendor_offers WHERE product_id = 'b55b1b248e40be084c426f763a10b1bb';
DELETE FROM product_variants WHERE product_id = 'b55b1b248e40be084c426f763a10b1bb';
DELETE FROM product_media WHERE product_id = 'b55b1b248e40be084c426f763a10b1bb';
DELETE FROM wishlists WHERE product_id = 'b55b1b248e40be084c426f763a10b1bb';
DELETE FROM product_reviews WHERE product_id = 'b55b1b248e40be084c426f763a10b1bb';
DELETE FROM products WHERE id = 'b55b1b248e40be084c426f763a10b1bb';
```

## Part 2 — QA fix matrix (live production)

| ID | Result | Evidence |
|----|--------|----------|
| BUG-01 | **FAIL** | 375px viewport: hamburger present (`Hiện điều hướng`) but `scrollWidth=555` > 375 — login CTA not hidden on xs, causes horizontal overflow. Screenshot: `tmp/post-merge-smoke/mobile-header-375.png` |
| BUG-02 | **PASS** | Tour step title `Cửa hàng` / `Danh mục tính năng`; no literal `Popover Title` |
| BUG-03 | **PASS** | `?tour=tour.features` auto-starts features tour; overlay closes on outside click (`overlayClickBehavior: close`) |
| BUG-04 | **PASS** | VI copy includes `chọn lựa`; no erroneous `lọa` |
| BUG-05 | **PASS** | Login error: `Email hoặc mật khẩu không đúng.` on `?lang=vi` |
| BUG-06 | **PASS** | Cart eyebrow: EN `1 item`; VI `1 sản phẩm` |
| BUG-07 | **PASS** | No DEC stubs; shipping `<select>` readable (`color: rgb(69, 33, 14)`) |
| BUG-08 | **PASS** | Catalog titles use `truncate` + `text-overflow: ellipsis` on card `<p>` |
| BUG-09 | **PASS** | Demo product removed from DB + API (10 titles, 0 demo slugs) |
| BUG-10 | **PASS** | `/login`, `/register`: single `<header>` (auth uses `luxury-shell`, no double chrome) |
| BUG-11 | **PASS** | `/membership?lang=vi` fully Vietnamese (no English Membership/Subscribe leaks) |
| BUG-12 | **PASS** | Product title links ≥44px height (e.g. 44×301px) |
| BUG-13 | **PASS** | Cart qty `aria-label="Quantity for Cho Tôi Xin Một Vé Đi Tuổi Thơ"` |
| BUG-14 | **PASS** | `GET /api/auth/me` → 200 `{user:null}`; no 401 console errors on guest pages |
| BUG-15 | **PASS*** | Plural `10 titles on the shelf` correct; singular key `shelfCountOne` in code (`count === 1`). *Not re-verified with exactly 1 product on prod (10-book catalog). |
| BUG-16 | **PASS** | `Xem hướng dẫn` disabled while tour active |
| BUG-17 | **PASS** | Close button aligned (Δtop ≈ 10px vs popover) |
| BUG-18 | **PASS** | `GET /api/catalog/search?q=hoang` → 200 |

### Additional smoke

| Check | Result |
|-------|--------|
| Guest `/api/auth/me` | 200, `user: null` |
| `/wishlist` redirect | → `/login?redirect=%2Fwishlist` |
| `/account` redirect | → `/login?redirect=%2Faccount` |
| Key pages HTTP | `/`, PDP, cart, membership, login, register, features, forgot — 200 |
| Console errors (guest) | None on `/`, `/login`, `/register`, `/cart`, `/membership`, `/features` |
| Portals (admin/vendor/…) | **Blocked** — no production operator credentials in session |
| Vercel runtime logs | **Not run** — `VERCEL_TOKEN` unset locally |

## Part 3 — Regression fix

**BUG-01 fix** (branch `fix/TASK-UI-005-mobile-header-overflow`): hide guest login button on xs (`hidden sm:inline-flex`) — login remains in mobile menu dropdown.

## Operator follow-ups

1. **Merge + deploy** mobile header fix PR when approved.
2. **Optional:** verify BUG-15 singular copy with a 1-title catalog slice (or staging).
3. **Portals:** run signed-in RBAC matrix when operator creds available.
