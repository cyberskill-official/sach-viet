import { MembershipPanel } from "@/components/membership-panel";

export const metadata = {
  title: "Sách Việt Circle · Membership",
  description: "Exclusive membership for curated Vietnamese literature and early releases.",
};

export default function MembershipPage() {
  return <MembershipPanel />;
}
