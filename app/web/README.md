# SachViet web application

This is the single full-stack Next.js application for the SachViet greenfield rebuild.

Run `npm run lint`, `npm run test`, `npm run verify`, and `npm run build` before review. See `OPERATIONS.md` for package and preview constraints.
