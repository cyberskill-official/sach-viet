window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the shared identity, login, portal-route guard, server authorization, and record-ownership foundation inside the full-stack Next.js application. It must enforce the documented single-role model across later portals. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>Every portal has distinct allowed roles and protected server routes, while the documented design deliberately uses one role string on each user instead of a generic permission matrix. The greenfield Next.js workspace has no identity or authorization code. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>The old handoff implemented login through Laravel Sanctum and a Nuxt proxy. The operator replaced that split stack with one Next.js application, so the same browser security property must be provided by an opaque httpOnly session cookie owned by Next.js. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Implement the documented role set in <code>app/web</code> with a SQLite user and session store, email-and-password login, per-email throttling, opaque httpOnly session cookies, server-side role checks, and reusable ownership helpers. Use Node.js built-in SQLite and cryptographic password hashing so the foundation does not need an external identity provider or database service. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Bootstrap the first administrator only when the authorized deployment supplies <code>BOOTSTRAP_ADMIN_EMAIL</code>, <code>BOOTSTRAP_ADMIN_PASSWORD_HASH</code>, and <code>AUTH_SESSION_SECRET</code> as platform secrets. Do not add secret values, deploy, or create a live administrator in this task. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Retain Laravel Sanctum and the Nuxt proxy. This is rejected because the operator selected one full-stack Next.js application. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Use OAuth, passwordless email, or an external identity provider. This is deferred because each option requires provider credentials or a delivery service that is not authorized for this foundation. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Migrate WordPress password hashes during this task. This is rejected because the operator chose a rebuild and no legacy migration access or data action is in scope. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the Next.js application has no identity implementation. Target: every documented role has a server-side access check and unauthorized portal or server access is rejected. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: browser JavaScript must not receive a bearer token or password hash. Target: authentication uses only an httpOnly session cookie and the first-admin inputs are named deployment secrets without committed values. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>The task establishes shared access control in the Next.js application. It does not implement portal business workflows, external OAuth, email delivery, supplier scope, or imported WordPress data migration. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Implement the documented role tree and route and server-guard mapping. &lt;!-- authority: llm-explicit --&gt;</li><li>Implement login, logout, per-email throttling, opaque session handling, and unauthorized redirects. &lt;!-- authority: llm-explicit --&gt;</li><li>Create a SQLite user and session store plus first-admin bootstrap support that reads platform secrets only at runtime. &lt;!-- authority: human-confirmed --&gt;</li><li>Establish reusable role and ownership-policy helpers for later domain tasks. &lt;!-- authority: human-confirmed --&gt;</li><li>Add checks for authorized access, unauthorized access, session expiry, and secret-free output. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Add a permission matrix, external OAuth providers, email delivery, or unlisted roles. &lt;!-- authority: llm-explicit --&gt;</li><li>Place session values, bearer tokens, passwords, or password hashes in browser JavaScript. &lt;!-- authority: llm-explicit --&gt;</li><li>Supply bootstrap secret values, deploy, or create a live administrator. &lt;!-- authority: human-confirmed --&gt;</li><li>Implement support-ticket, goods-request, or other domain behavior before their tasks. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>TASK-REBUILD-001 provides the full-stack Next.js workspace, Node 24 runtime, container package, and quality-command baseline. The role mapping in the handoff remains the role authority for this task. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented role contract and approved Next.js architecture into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task does not invent role names, provider credentials, bootstrap values, external identity contracts, or legacy migration access. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield authoring plan, selected full-stack Next.js, and approved recommended defaults. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
