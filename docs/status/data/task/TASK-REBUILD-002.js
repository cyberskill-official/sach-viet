window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the shared identity, login, portal-route guard, API authorization, and record-ownership foundation for the greenfield platform. It must enforce the documented single-role model across every later portal. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>Every portal has distinct allowed roles and protected API routes, while the documented design deliberately uses one role string on each user rather than a generic permission matrix. The greenfield workspace has no identity or authorization code yet. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>The documented auth flow keeps the API token in an httpOnly cookie through the Nuxt proxy. It also requires per-email login throttling, ownership policies for sensitive records, and legacy WordPress password compatibility for imported users. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Implement the source-confirmed user role set, Sanctum-based login flow, Nuxt proxy token handling, frontend portal guards, and API role middleware. Implement the documented ownership-policy pattern only for support-ticket and goods-request records once their domain tasks add those records. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Use the documented role tree and redirects as the authorization contract. Preserve the no-token-in-browser-JavaScript property and reject access outside each portal's allowed roles. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Use a generic permission package or matrix. This was rejected because the handoff explicitly defines the single-role model as the intended capability structure. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Store bearer tokens in browser JavaScript. This was rejected because the documented Nuxt proxy and httpOnly cookie pattern exists to reduce token exposure. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the new workspace has no identity or access-control implementation. Target: each documented role has a source-selected login and route-access check, while unauthorized portal and API access is rejected. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: the handoff requires browser JavaScript to avoid holding the Sanctum token. Target: the new auth flow keeps the token behind the documented proxy and httpOnly cookie boundary. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>The task establishes the shared access-control foundation. It does not implement portal business workflows, external OAuth, supplier scope, or imported WordPress data migration. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Implement the documented role tree and its route and API guard mapping. &lt;!-- authority: llm-explicit --&gt;</li><li>Implement login, logout, throttling, authenticated proxy handling, and unauthorized redirects. &lt;!-- authority: llm-explicit --&gt;</li><li>Establish reusable role and ownership-policy patterns for later domain tasks. &lt;!-- authority: human-confirmed --&gt;</li><li>Add source-selected checks for authorized and unauthorized access. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Add a permission matrix, external OAuth providers, or unlisted roles. &lt;!-- authority: llm-explicit --&gt;</li><li>Place API tokens in browser JavaScript or commit authentication credentials. &lt;!-- authority: llm-explicit --&gt;</li><li>Implement support-ticket and goods-request domain behavior before their task. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>TASK-REBUILD-001 provides the Nuxt and Laravel workspace that this task configures. The role mapping in the handoff is the only role authority for this task. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the approved handoff role and auth contract into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task does not invent role names, redirects, API paths, permission policies, or external provider credentials. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield authoring plan before this task was drafted. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
