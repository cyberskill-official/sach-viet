window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-023"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Prove greenfield B2C capability coverage with an evidence matrix and automated fixture/API checks, and author a cutover plan that records go/no-go gates without executing production cutover, DNS changes, or WordPress retirement. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The north star requires the new B2C storefront to reach feature parity with the live WordPress store before <code>sachviet.us</code> cutover, while WordPress remains live until replacement and the plan is parity-then-cutover rather than a hybrid frankenstein. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Greenfield Tasks 1–22 shipped catalog, cart/checkout, support, admin commerce, vendor payouts, WP import compatibility, and a quality/preview-release bar — but there is no single closed B2C evidence matrix that maps source-confirmed storefront flows to greenfield proof status, and no cutover plan artefact that lists unmet gates without inventing a go decision. Non-rebuild <code>TASK-CUTOVER-001</code> / <code>TASK-CUTOVER-002</code> stay on hold under the greenfield-only decision and must not be reopened. Live WordPress comparison data is unavailable in this session, so claiming live WP parity would be fabrication. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a <code>b2c-parity-cutover-core</code> module (or equivalent) in <code>app/web</code> plus committed plan artefacts under the task <code>ship/</code> folder that:</p>\n<ol><li>Defines a closed B2C capability checklist derived from source-confirmed <code>/ecom</code> storefront areas and the B2C-supporting commerce surfaces already in rebuild scope (catalog browse/detail, cart, hosted checkout, order history, auth, support tickets, goods requests, vendor self-registration, admin vendor approval/payouts, multi-vendor offers, Vietnamese search, WP import compatibility, quality/preview bar). Each row MUST carry exactly one status from the closed set: <code>greenfield_proven</code> | <code>source_gap</code> | <code>evidence_unavailable</code> | <code>deferred_out_of_scope</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Produces a B2C evidence matrix artefact that distinguishes (a) rows proven by greenfield fixtures/tests/verify scripts, (b) source-documented flows still marked gap, (c) rows that cannot be verified without live legacy comparison data (<code>evidence_unavailable</code>), and (d) explicitly deferred items. The matrix MUST NOT assert <code>live_wp_parity</code> or any equivalent live-parity claim. &lt;!-- authority: human-confirmed --&gt;</li><li>Produces a cutover plan artefact that records the gate checklist for a future owner go/no-go (parity evidence packet complete, quality/preview bar green, backup verified, named rollback plan, owner go decision, separate deployment instruction) without executing any gate, inventing cutover windows/DNS plans/credentials, or treating this task status as deployment authority. Adapt the gate language to greenfield Next.js <code>app/web</code> (not Nuxt/Laravel revival). &lt;!-- authority: llm-explicit --&gt;</li><li>Provides automated checks that (a) the checklist is complete and every row has a closed-set status, (b) every <code>greenfield_proven</code> row cites an existing greenfield verify script or core module evidence key, (c) the matrix rejects unknown statuses and forbids live-parity claim fields, (d) the cutover plan refuses production actions in the default path (<code>refused_production</code> / equivalent), and (e) on-hold cutover/migration task paths remain untouched. &lt;!-- authority: human-confirmed --&gt;</li><li>Wires a verify script into <code>npm run verify</code> and documents the evidence-matrix + cutover-plan outcomes in <code>OPERATIONS.md</code> (or equivalent) as planning artefacts, not as production authorization. &lt;!-- authority: llm-explicit --&gt;</li></ol>\n<p>Leave Tasks 1–22 product cores intact aside from minimal verify/OPERATIONS wiring. Leave <code>TASK-CUTOVER-001</code>, <code>TASK-CUTOVER-002</code>, and <code>TASK-MIGRATION-001</code> on hold. Do not push, deploy, merge, change DNS, retire WordPress, or revive WordPress/WooCommerce/Dokan as the product runtime. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Claim live WordPress B2C feature parity from greenfield tests alone. This is rejected because live legacy comparison data is unavailable and fabricating parity would violate anti-fabrication and the cutover readiness doctrine. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Reopen and implement <code>TASK-CUTOVER-001</code> / <code>TASK-CUTOVER-002</code> instead of a greenfield rebuild task. This is rejected by standing orders to leave non-rebuild <code>on_hold</code> work alone; this task supersedes only by producing greenfield-owned evidence/plan artefacts, not by mutating cutover specs. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Execute DNS cutover, CapRover production promote, or WordPress retirement once the matrix looks complete. This is rejected because sources require separate owner go + deployment instruction, and standing orders forbid push/deploy/merge in this session. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Require CapRover <code>sachviet-current</code> / live WooCommerce admin access before CI can pass. This is rejected because greenfield CI must stay credential-free and fixture-driven. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Build a hybrid frankenstein that keeps WordPress plugins in the product path. This is rejected by the known dead-end: parity-then-cutover, not piecemeal Dokan/WP replacement. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: greenfield has B2C cores and import/quality bars but no closed evidence matrix or cutover plan with automated coverage checks. Target: tests/verify prove the closed B2C checklist is populated with only closed-set statuses, every <code>greenfield_proven</code> row cites greenfield evidence, live-parity claims are refused, cutover plan lists unmet gates without executing them, and <code>npm run verify</code> includes the new verifier. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: standing orders forbid push/deploy/merge and live cutover; cutover/migration stay on hold; prior cores stay intact. Target: inspection/tests prove no DNS/production/WP-retirement action, no mutation of on-hold cutover/migration specs, no WordPress PHP runtime revival, no CapRover/GitHub credential invention, and Tasks 1–22 cores unchanged beyond minimal verify/OPERATIONS wiring. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes greenfield B2C capability evidence and a non-executing cutover plan. It does not claim live WordPress parity or perform production cutover. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Closed B2C capability checklist with closed-set row statuses. &lt;!-- authority: human-confirmed --&gt;</li><li>Evidence matrix artefact that forbids live WP parity claims and records <code>evidence_unavailable</code> where live comparison is missing. &lt;!-- authority: human-confirmed --&gt;</li><li>Cutover plan artefact listing go/no-go gates for greenfield Next.js without executing them. &lt;!-- authority: llm-explicit --&gt;</li><li>Automated core checks + verify script wired into <code>npm run verify</code>. &lt;!-- authority: llm-explicit --&gt;</li><li>OPERATIONS (or equivalent) documentation of matrix/plan as planning artefacts only. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Live WordPress/WooCommerce comparison, CapRover <code>sachviet-current</code> access, or production re-import. &lt;!-- authority: human-confirmed --&gt;</li><li>DNS changes, Cloudflare changes, production deploy/promote, traffic switch, or WordPress retirement. &lt;!-- authority: human-confirmed --&gt;</li><li>Pushing to GitHub, opening PRs, merging, or storing hosting credentials. &lt;!-- authority: human-confirmed --&gt;</li><li>Reopening or implementing <code>TASK-CUTOVER-001</code>, <code>TASK-CUTOVER-002</code>, or <code>TASK-MIGRATION-001</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Inventing cutover windows, DNS records, rollback runbooks with unverified backup locations, or owner go decisions. &lt;!-- authority: llm-explicit --&gt;</li><li>Expanding B2B, publisher, author, or royalty scope beyond B2C checklist coverage notes. &lt;!-- authority: llm-explicit --&gt;</li><li>Mutating Tasks 1–22 product cores beyond minimal verify/OPERATIONS wiring. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Tasks 5, 7, 8, 21, and 22 provide storefront commerce, admin commerce, vendor payouts, WP import compatibility, and the quality/preview-release bar that the evidence matrix cites. Non-rebuild cutover tasks remain on hold and are documentation siblings only. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented B2C storefront/cutover north star, greenfield-only constraints, and standing orders into this rebuild evidence-and-plan task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task produces greenfield B2C capability evidence and a non-executing cutover plan; it excludes live WP parity claims, production cutover, and on-hold cutover/migration implementation. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue, session-wide routine acceptance gates, and standing orders to never push/deploy/merge, leave non-rebuild on_hold alone, and pause only for real decisions. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-023.</em></p>";
