window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SECURITY-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>After verified vendor-dashboard completion, identify and retire only the source-confirmed exposed maintenance debug endpoints. Preserve required vendor behavior and do not treat the documented HMAC internal automation routes as the same route set without recovered source evidence. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The handoff reports that HMAC-protected maintenance debug endpoints under <code>/admin/maintenance/*</code> remain exposed and should be removed after vendor dashboard verification. The roles documentation separately describes HMAC-protected <code>/internal/*</code> routes for maintenance and automation without admin sessions. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>The available repository does not contain route definitions, methods, callers, HMAC implementation details, vendor-dashboard dependencies, or replacement operations needed to determine the exact exposed endpoints. Those details must come from discovery and verified prerequisites instead of assumption. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>After <code>TASK-VENDOR-001</code> and <code>TASK-VENDOR-002</code> are complete and discovery identifies the source route set and callers, retire exposure only for the exact maintenance endpoints established by recovered source. Add source-selected negative coverage and preservation checks for required vendor-dashboard behavior. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Do not remove every HMAC internal route, infer a method or namespace, add a replacement operation, or change an access policy without evidence. If callers or required operations are unclear, record the evidence gap and request a decision before retiring an endpoint. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Remove every HMAC-protected internal route. This was rejected because HMAC automation is documented as an existing security control and the source does not identify the full route set as exposed debug endpoints. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Retire endpoints before vendor-dashboard verification. This was rejected because the handoff makes verified vendor-dashboard completion an explicit prerequisite. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the handoff records exposed HMAC maintenance debug endpoints. Target: the source-identified maintenance endpoints are no longer exposed after verified vendor-dashboard completion, while required vendor behavior remains verified. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: HMAC-keyed internal API routes are documented as an automation control, but the exact relationship to maintenance endpoints is not established. Target: the work preserves source-confirmed required internal automation behavior and does not remove a route outside the recovered maintenance set. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>The scope retires only the source-confirmed exposed maintenance endpoint set after vendor prerequisites are complete. It excludes a general HMAC route removal, replacement operations, and new access policy work. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Verify completion of <code>TASK-VENDOR-001</code> and <code>TASK-VENDOR-002</code> before implementation begins. &lt;!-- authority: human-confirmed --&gt;</li><li>Locate the exact maintenance route set, callers, and relevant vendor-dashboard behavior from recovered source. &lt;!-- authority: llm-explicit --&gt;</li><li>Retire only source-confirmed exposed maintenance endpoints and add source-selected negative and preservation checks. &lt;!-- authority: human-confirmed --&gt;</li><li>Record an evidence gap and request a decision when a caller or required operation is unclear. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Remove every HMAC-protected internal route, add a replacement endpoint, or change the HMAC access policy without source evidence. &lt;!-- authority: llm-explicit --&gt;</li><li>Change vendor-dashboard behavior or start vendor dashboard analytics work. &lt;!-- authority: llm-explicit --&gt;</li><li>Run the application locally, commit credentials, or deploy without operator instruction. &lt;!-- authority: human-edited --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-VENDOR-001</code> and <code>TASK-VENDOR-002</code> must both be complete before implementation begins. <code>TASK-VENDOR-002</code> remains subject to its own owner-approved KPI decision, so this task cannot begin until that decision-backed work and vendor verification are complete. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Discovery must identify the source route set and callers, or record them unavailable, before any endpoint retirement decision. This is a source-evidence requirement rather than a dependency on an unnamed team. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex assisted with converting handoff documentation and approved defaults into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task does not invent route methods, namespaces, callers, HMAC implementation details, replacement operations, or access policies that the source does not establish. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: An operator must review the task before implementation begins. &lt;!-- authority: human-edited --&gt;</li></ul>\n<h2>Closure</h2>\n<p>Closed as superseded by <code>TASK-SEC-002</code> (CSRF, vendor-offer RBAC, AI allowlist) and <code>TASK-GOV-001</code>. No HMAC <code>/admin/maintenance/*</code> routes exist in this repository. &lt;!-- authority: llm-explicit --&gt;</p>";
