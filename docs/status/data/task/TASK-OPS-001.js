window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OPS-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>After a short dependency check, Production-retire WordPress import apply, admin AI settings/chat, and the supplier portal from UI and reachable routes (<code>PKG-10</code> slice). Keep WordPress fixture import for tests if REBUILD-021 still needs it. Do not delete migrations 001–003. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p><code>GAP-OPS-001</code> / <code>PKG-10</code> / <code>FL-PLT-09</code> / <code>FL-ADM-11</code> still leave WordPress apply, admin AI, and supplier reservation reachable or advertised. TASK-SEC-002 already 410s Production AI chat unless <code>AI_CHAT_ENABLED=1</code>; TASK-GOV-001 gates apply behind the commerce kill-switch. The golive wave wants those surfaces retired from Production UI, not merely flagged. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Run a short dependency check (admin dashboard links, seed, verify scripts). Then gate or return 410 on Production for <code>POST /api/admin/wordpress-import/apply</code>, <code>/api/admin/ai-settings</code>, <code>/api/admin/ai/chat</code>. Hide supplier portal / <code>employee_supplier</code> from nav; matcher returns 404/410. Stop advertising CapRover/SQLite as a path in <code>app/web/OPERATIONS.md</code>. Keep WordPress <strong>fixture</strong> import in tests only if still required for the REBUILD-021 verifier. Do not delete 001–003 migrations. Do not execute WordPress DNS cutover (<code>TASK-CUTOVER-*</code> stay on_hold). &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: Production 410/hidden for WP apply, admin AI settings/chat, supplier portal; local/test fixture import may remain. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Delete import migrations and REBUILD-021 fixtures. Rejected — plan says keep 001–003 and test fixtures if the verifier still needs them. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Reopen TASK-SUPPLIER-001 or TASK-CUTOVER-002. Rejected — supplier has no replacement portal this wave; DNS cutover stays on_hold. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: WP apply, admin AI, and supplier remain reachable or linked in Production UI. Target: Production UI and those APIs are 410/hidden after the dependency check; tests cover the denials; <code>npm test</code> passes. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: no Production deploy, merge, or DNS change from this task; no deletion of migrations 001–003. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Dependency check of admin dashboard links, seed, and verify scripts. &lt;!-- authority: llm-explicit --&gt;</li><li>Production gate or 410: <code>/api/admin/wordpress-import/apply</code>, <code>/api/admin/ai-settings</code>, <code>/api/admin/ai/chat</code>. &lt;!-- authority: llm-explicit --&gt;</li><li>Hide supplier portal / <code>employee_supplier</code> from nav; matcher 404/410. &lt;!-- authority: llm-explicit --&gt;</li><li>OPERATIONS.md must not advertise CapRover/SQLite as a supported path. &lt;!-- authority: llm-explicit --&gt;</li><li>Tests: handler status codes for retired Production routes (extend existing HTTP/core tests; no new source-grep <code>*-route.test.mjs</code>). &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>WordPress DNS / <code>TASK-CUTOVER-*</code>, deleting 001–003 migrations, live payment keys, invented finance numbers, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-GOV-001</code> and <code>TASK-SEC-002</code> are the in-flight audit set (kill-switch + AI allowlist). This task is eligible only after those complete HITL (<code>testing → done</code>). HITL remains required at reviewing → ready_to_test and testing → done. Do not request HITL while authoring. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor agent authoring the local-complete golive wave queue. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates after implementation. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
