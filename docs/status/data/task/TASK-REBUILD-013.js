window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-013"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield B2B quote pipeline in <code>app/web</code>: organizations, institution selection lists, <code>B2bQuote</code> records with the documented draft → sent → negotiating → won/lost lifecycle, staff pipeline reads with quote click-through, and institution-owned quote reads under signed sessions — without disclosing upstream suppliers (blind brokerage). &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The vision describes blind institutional brokerage: institutions build selection lists, request quotes, and B2B staff quote and negotiate before PO-based ordering. The data model names <code>Organization</code>, <code>SelectionList</code> / <code>SelectionListItem</code>, and <code>B2bQuote</code> with pipeline states draft → sent → negotiating → won/lost. The <code>/b2b</code> portal needs a quote pipeline with click-through to quote management; quote → order conversion and contract/PO artifacts are later work. The greenfield application has identity, portals, and catalog, but no B2B quote domain. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite B2B quote repository and signed-session route handlers in <code>app/web</code>. Persist organizations and membership links so <code>school_librarian</code> actors belong to an institution. Persist selection lists and list items that reference catalog products. Persist <code>b2b_quotes</code> with the closed status set <code>draft</code>, <code>sent</code>, <code>negotiating</code>, <code>won</code>, and <code>lost</code>, plus quote line items that reference products and optional negotiated unit prices as non-negative USD decimal strings matching existing commerce money conventions. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Allow <code>school_librarian</code> (and <code>admin</code>) actors to create and list selection lists for their organization, request a quote from a selection list (creating a <code>draft</code> quote), and read quotes owned by their organization. Allow <code>employee_b2b</code> and <code>admin</code> actors to list the pipeline grouped by status, read any quote detail (click-through target), and advance quote status only along the closed transitions <code>draft→sent</code>, <code>sent→negotiating</code>, <code>negotiating→won</code>, and <code>negotiating→lost</code>. Institution-facing quote reads and responses MUST omit vendor ids, supplier identifiers, payout facts, and other upstream seller disclosure so the brokerage stays blind. Emit safe structured events that omit session tokens, email addresses, request bodies, and payment secrets. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Implement quote → order conversion, contracts, or purchase-order artifacts now. This is deferred because the source and rebuild sequence reserve those for Task 14. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Build institution budget tracking, PO submission, or MARC delivery inside this task. This is deferred because those belong to the institution buyer portal work (Task 15) and are not defined as quote-pipeline scope. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Disclose product vendor offers or supplier identity on institution quote views. This is rejected because the source defines B2B brokerage as blind. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Recover a legacy Laravel/Nuxt <code>quotes_pipeline</code> implementation. This is rejected under the greenfield-only rebuild decision. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the greenfield application has no organizations, selection lists, B2B quotes, or staff pipeline. Target: tests prove an institution buyer can create a selection list, request a draft quote, and read organization-owned quotes; a B2B staff actor can list the pipeline by status, open quote detail, and advance status only on the closed transitions; unauthorized roles cannot. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: brokerage is blind and order/contract conversion is later work. Target: tests prove institution quote responses omit vendor/supplier identifiers, the implementation creates no order, contract, or PO records, and responses and structured events omit session tokens, email addresses, request bodies, and payment secrets. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes the greenfield quote pipeline, selection lists, organizations, and role-gated reads/writes under signed sessions. It does not invent order conversion, contracts, POs, budgets, MARC delivery, or deployment. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Organizations and membership links for institutional buyers. &lt;!-- authority: human-confirmed --&gt;</li><li>Selection lists and product-backed list items. &lt;!-- authority: human-confirmed --&gt;</li><li><code>B2bQuote</code> persistence with closed statuses draft, sent, negotiating, won, and lost, plus product line items and optional negotiated USD unit prices. &lt;!-- authority: human-confirmed --&gt;</li><li>Institution create/list selection lists, request quote from a list, and read organization-owned quotes without supplier disclosure. &lt;!-- authority: human-confirmed --&gt;</li><li>B2B staff pipeline list grouped by status, quote detail click-through, and closed status transitions. &lt;!-- authority: human-confirmed --&gt;</li><li>Signed-session authorization for <code>school_librarian</code> / <code>admin</code> on institution surfaces and <code>employee_b2b</code> / <code>admin</code> on staff surfaces. &lt;!-- authority: human-confirmed --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, and payment secrets. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Quote → order conversion, commerce order creation from a won quote, contracts, or purchase-order artifacts. &lt;!-- authority: human-confirmed --&gt;</li><li>Institution budget tracking, PO submission UX, MARC record delivery, or publisher/author earnings. &lt;!-- authority: llm-explicit --&gt;</li><li>Disclosing vendor offers, supplier identities, or payout facts to institution buyers. &lt;!-- authority: human-confirmed --&gt;</li><li>Legacy WordPress/Laravel/Nuxt recovery, live notification changes, email/Zalo, or deployment. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 2 provides signed sessions and role normalization, including <code>school_librarian</code>, <code>employee_b2b</code>, and portal ACL maps for <code>/institution</code> and <code>/b2b</code>. Task 3 provides shared portal foundations. Task 4 provides catalog products that selection-list and quote line items reference. Later Task 14 may convert won quotes into orders and attach contract/PO artifacts. Later Task 15 may deepen the institution buyer portal with budget and PO submission. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented B2B brokerage, portal, and data-model sources into this greenfield rebuild task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task builds the quote pipeline and selection-list core and excludes order conversion, contracts, POs, budgets, MARC delivery, and supplier disclosure. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue and session-wide routine acceptance gates. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-013.</em></p>";
