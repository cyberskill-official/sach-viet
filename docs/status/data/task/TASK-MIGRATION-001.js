window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MIGRATION-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Classify and reconcile source-visible unmatched WordPress order items after discovery identifies the current <code>WpImport</code> source and legacy import data. Preserve legacy identifiers and make no destructive data change without the required backup and operator instruction. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The handoff reports 324 unmatched WordPress order items caused by Vietnamese slug edge cases. WordPress remains the live legacy store while one-way <code>WpImport</code> continues to sync data into the new platform. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Imported rows carry legacy identifiers, and the documented <code>WpImport</code> order match uses billing email to user ID plus total amount. The available repository does not contain the current import source, legacy data, mismatch set, or matching details needed to name a safe reconciliation action. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>After <code>TASK-DISCOVERY-001</code> identifies the <code>WpImport</code> source and available legacy import data, classify each source-visible unmatched item and record a source-derived reconciliation outcome. Preserve legacy identifiers and use only recovered matching behavior. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Do not bulk-match by slug heuristics, alter matching rules without evidence, or run a production re-import as verification. If a persistent reconciliation action is proposed, require the project backup convention and explicit operator instruction before it occurs. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Bulk-match records by slug heuristics. This was rejected because the documented mismatch cause does not establish safe matching behavior and an incorrect match can corrupt imported data. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Redesign the import model before examining the current source and data. This was rejected because the documented data flow and legacy identifiers must first be preserved and understood. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the handoff reports WordPress order items unmatched because of Vietnamese slug edge cases. Target: each source-visible unmatched item has a source-derived classification and recorded reconciliation outcome without unapproved destructive data changes. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: WordPress remains the live legacy store and imported rows carry legacy identifiers. Target: the work preserves source-confirmed legacy identifiers, matching behavior, and current production data unless an operator separately authorizes a backed-up persistent change. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>The scope classifies and reconciles source-visible legacy mismatches with recovered import evidence. It excludes import-model redesign, unverified matching rules, and production re-import. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Locate the current <code>WpImport</code> source, legacy import data, and source-visible mismatch set from the discovery output. &lt;!-- authority: llm-explicit --&gt;</li><li>Classify each source-visible unmatched item and record a source-derived reconciliation outcome. &lt;!-- authority: human-confirmed --&gt;</li><li>Preserve legacy identifiers and observed import matching behavior. &lt;!-- authority: human-confirmed --&gt;</li><li>Record an evidence gap when source data does not establish a safe outcome. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Redesign the import model, change matching rules without source evidence, or bulk-match by slug heuristics. &lt;!-- authority: llm-explicit --&gt;</li><li>Run a production re-import or other destructive data action as part of verification. &lt;!-- authority: human-edited --&gt;</li><li>Make a persistent data change without the required backup and explicit operator instruction. &lt;!-- authority: human-edited --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-DISCOVERY-001</code> must identify the application path, current <code>WpImport</code> source, legacy import data, and mismatch evidence, or record each unavailable, before implementation starts. This is a discovery result rather than a dependency on an unnamed team. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Any persistent reconciliation action needs the project's required backup and explicit operator instruction. This is an execution control, not authorization to change production data. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex assisted with converting handoff documentation and approved defaults into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task does not invent a matching rule, source record, slug heuristic, reconciliation outcome, import path, or destructive data action that the source does not establish. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: An operator must review the task before implementation begins. &lt;!-- authority: human-edited --&gt;</li></ul>";
