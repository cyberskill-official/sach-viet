window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLT-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Ship Phase 2 foundations <strong>delta only</strong> on Vercel + Supabase APAC: Supabase Storage package scaffolding + migration, Auth migration plan (keep <code>sv_session</code>), private <code>app</code> schema strategy (no cutover), and practical observability fingerprints. Defer US-region per <code>DEC-OPS-001</code>. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p>Adjusted tracker Phase 2 still lists Storage, Auth plan, <code>app</code> schema strategy, and observability as remaining while interim DECs now allow non-rate foundation work. Forcing Auth or <code>app</code> schema cutover would violate <code>DEC-OPS-001</code>. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<ol><li>Additive migration expanding <code>stored_objects</code> metadata for a future private Supabase Storage backend; keep Postgres BYTEA as the active backend. &lt;!-- authority: llm-explicit --&gt;</li><li>Document Auth and <code>app</code> schema migration packages without cutting over. &lt;!-- authority: llm-explicit --&gt;</li><li>Extend readiness/liveness with release SHA, deployment env, schema name, and storage mode (no secrets). &lt;!-- authority: llm-explicit --&gt;</li></ol>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Storage scaffold migration + adapter/docs; Auth plan doc; <code>app</code> schema strategy doc; observability on <code>/api/health</code> and <code>/api/ready</code>. &lt;!-- authority: llm-explicit --&gt;</li><li>Tracker + DEC blocker updates. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Supabase Auth cutover, <code>public</code> → <code>app</code> cutover, US-region move, live payments, tax/shipping invention, Production deploy, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Success Metrics</h2>\n<p>Primary: Phase 2 delta docs + scaffold landed; ready/health expose safe fingerprints; DB backend still default. Guardrail: no Auth/<code>app</code>/US cutover; no live keys. &lt;!-- authority: llm-explicit --&gt;</p>";
