window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEST-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Add Playwright (Chromium only this wave) against <code>next start</code> + Compose Postgres for FL happy/denial smokes: register → verify (test-hook token) → login → catalog → sandbox checkout → order list; customer denied vendor offer write; admin login. Keep Node <code>--test</code> cores. Do not add source-grep <code>*-route.test.mjs</code>. Do not run the full 99×suffix matrix. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p>TASK-TEST-001 adds handler HTTP tests and a hard <code>smoke:production</code> gate. <code>PKG-72</code> still needs a browser + built-server slice. The golive wave starts that pyramid with Chromium happy/denial paths only, not every <code>TC-*</code> suffix. Source-grep route tests must not grow. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Keep existing Node <code>--test</code> core/HTTP suites. Add Playwright Chromium against <code>next start</code> and Compose Postgres. Cover: register → verify via test-hook token → login → catalog → checkout sandbox stub → order list; vendor offer write denied for customer; admin login. Extend local <code>smoke:docker</code> to hit <code>/api/ready</code> (TASK-PLT-001). Lint/test/verify/build must be green before review. Refuse <code>sk_live_</code> and <code>PAYPAL_MODE=live</code>. No merge/deploy from this task. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: exercised through the real app; assertions are HTTP/UI behavior, not source strings. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Implement the full 99-flow × suffix Playwright matrix now. Rejected — golive wave is PKG-72 start, not the full pyramid. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Add more <code>*-route.test.mjs</code> source greps. Rejected — plan forbids new source-grep route tests. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: no Playwright <code>next start</code> happy-path suite. Target: named Chromium smokes pass against Compose Postgres; <code>smoke:docker</code> hits <code>/api/ready</code>; no new source-grep route tests. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: no Production deploy or merge from this task; no <code>sk_live_</code> / <code>PAYPAL_MODE=live</code>; skipped required smoke checks still fail. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Playwright Chromium only: register → verify (test hook) → login → catalog → sandbox checkout → order list. &lt;!-- authority: llm-explicit --&gt;</li><li>Denial: customer cannot write vendor offers; admin can log in. &lt;!-- authority: llm-explicit --&gt;</li><li>Keep Node <code>--test</code> cores; no new <code>*-route.test.mjs</code> greps. &lt;!-- authority: llm-explicit --&gt;</li><li><code>smoke:docker</code> includes <code>/api/ready</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Full 99×suffix matrix, Firefox/WebKit this wave, live keys, Production deploy, WordPress DNS, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-TEST-001</code> is in-flight. <code>TASK-UI-002</code>, <code>TASK-COM-002</code>, <code>TASK-PLT-001</code>, and <code>TASK-JOB-002</code> are this wave and must complete HITL before this task is eligible (<code>depends_on</code> not yet <code>done</code>). HITL remains required at reviewing → ready_to_test and testing → done. Do not request HITL while authoring. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor agent authoring the local-complete golive wave queue. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates after implementation. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
