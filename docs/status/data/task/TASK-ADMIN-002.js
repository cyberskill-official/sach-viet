window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-ADMIN-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Expose admin-only HTTP APIs and a thin <code>/admin</code> catalog section that wrap existing <code>catalog-core</code> writers so Day-2 Production catalog load can create categories, products (with optional media/variant), and vendor offers without WordPress import or <code>seed:local</code>. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>Production health is green and the public catalog is empty. Ops docs recommend admin day-2 entry for the first Production catalog, but admin surfaces today only cover dashboard, vendor applications, payouts, and WordPress import status. Catalog writes already exist in <code>catalog-core.mjs</code> (<code>createCategory</code>, <code>createProduct</code>, <code>addProductMedia</code>, <code>createProductVariant</code>, <code>writeVendorOffer</code>) with no admin HTTP or UI wrapper. Public catalog routes are read-only; <code>POST /api/vendor/offers</code> requires an existing product. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add admin-gated routes under <code>/api/admin/catalog/</code> that authenticate via the existing session cookie pattern, require <code>admin</code> (via the same administrator check used by admin commerce), and call <code>catalog-core</code> writers/readers. Extend <code>admin-dashboard.tsx</code> with a thin catalog section: list current categories/products summary and create category → product (optional media/variant) → offer. Day-2 first-party offers MAY use the logged-in admin user id as <code>vendorId</code> (or an explicit platform vendor id field); <code>vendor_offers.vendor_id</code> is unconstrained TEXT and admins already pass <code>canAccessOwnedRecord</code> for any vendor id. Update <code>OPERATIONS.md</code> Production catalog options so option 1 is admin day-2 and WordPress fixture is not the recommended path. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Run <code>seed:local</code> against Supabase Production. Rejected — standing Production rule forbids it. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Load via WordPress fixture import. Rejected for Day-2 default — WP stays N/A / not recommended; admin commerce path is the documented day-2 option. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Build a full vendor portal offer UI first. Rejected as out of scope for minimal Day-2 unblock. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: Production catalog empty and no admin write surface. Target: an authorized admin can create ≥1 category, ≥1 product (with variant when checkout requires it), and ≥1 active in-stock offer through the new APIs/UI, and public <code>GET /api/catalog/products</code> reflects the product after deploy. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: non-admin callers are refused on admin commerce routes; catalog-core keeps price/stock on offers. Target: non-admin callers receive 401/403; product create refuses offer fields; no WordPress, Stripe, or Phase B/C unlock; no <code>seed:local</code> against Production. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>This task owns the minimal admin catalog write path for Day-2. It does not unlock payments, WordPress cutover, or bulk import. &lt;!-- authority: human-confirmed --&gt;</p>\n<h3>In scope</h3>\n<ul><li><code>GET/POST /api/admin/catalog/categories</code> wrapping category list/create. &lt;!-- authority: human-confirmed --&gt;</li><li><code>GET/POST /api/admin/catalog/products</code> wrapping product list/create; product create MAY accept optional media and/or variant payloads (or sibling POSTs under the same admin catalog namespace). &lt;!-- authority: human-confirmed --&gt;</li><li><code>POST /api/admin/catalog/offers</code> wrapping <code>writeVendorOffer</code> for an admin actor. &lt;!-- authority: human-confirmed --&gt;</li><li>Thin catalog section on <code>admin-dashboard.tsx</code>: create category → product → offer; show a short current-catalog summary. &lt;!-- authority: human-confirmed --&gt;</li><li>Focused verify script and/or tests asserting admin gate + catalog-core reuse. &lt;!-- authority: llm-explicit --&gt;</li><li><code>OPERATIONS.md</code> Production catalog options: admin day-2 first; WP fixture not recommended; <code>seed:local</code> still forbidden on Production. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Bulk import, WordPress import apply, live WP MySQL migration. &lt;!-- authority: human-confirmed --&gt;</li><li>Stripe, Phase B/C unlock, vendor portal offer UI redesign. &lt;!-- authority: human-confirmed --&gt;</li><li>Running <code>seed:local</code> against Production, inventing admin credentials, or Production deploy/push/merge without operator instruction. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-REBUILD-004</code> supplies catalog-core writers and the product/offer split. <code>TASK-REBUILD-007</code> supplies admin-only commerce patterns. <code>TASK-PORTALUI-003</code> supplies the admin dashboard shell to extend. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor authored this task from the approved Day-2 admin catalog plan and existing catalog/admin sources. &lt;!-- authority: llm-explicit --&gt;</li><li><strong>Scope:</strong> No AI decision support, automated catalog generation, or credential invention is introduced. &lt;!-- authority: llm-explicit --&gt;</li><li><strong>Human review:</strong> Operator approved the Day-2 plan; HITL remains required at reviewing→ready_to_test and testing→done. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
