window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Align <code>super_admin</code> authorization with <code>admin</code> at every source-confirmed frontend and API guard. The approved default makes <code>super_admin</code> an exact alias of <code>admin</code>, without widening any other role or changing the authentication flow. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The role document says that <code>admin</code> is allowed everywhere, while <code>super_admin</code> is referenced only in the employee frontend middleware and should be treated as admin. The documented frontend and API guards otherwise list <code>admin</code> without <code>super_admin</code>. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>The application source is absent from this repository, so the exact role representation, guard helpers, redirects, and API middleware must be discovered before any code change. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>After <code>TASK-DISCOVERY-001</code> locates the guards and role representation, make <code>super_admin</code> receive every source-confirmed access decision that <code>admin</code> receives and no privilege that <code>admin</code> lacks. Apply the same alias rule to the recovered frontend and API guard families. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>If recovered source proves that <code>super_admin</code> has a distinct business or security meaning, stop and record the conflict instead of collapsing the roles. Preserve all existing non-admin denials, login redirects, policies, token handling, and public-route behavior. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Keep the current employee-only <code>super_admin</code> treatment. This was rejected because the handoff explicitly says to treat the role as admin. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Replace the single-role model with a permission matrix. This was rejected because the handoff describes a single-role capability model and advises retaining it. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: <code>super_admin</code> appears only in the employee frontend guard while the role document says to treat it as admin. Target: every source-confirmed privileged frontend and API guard family grants equivalent outcomes to admin and super_admin. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: route guards, policies, and the httpOnly-cookie auth flow are documented security controls. Target: source-selected checks confirm unchanged denial outcomes for unrelated roles and no change to login, token, cookie, or public-route behavior. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>The scope makes one documented role alias consistent across recovered guards. It excludes a role-model redesign, role-assignment changes, and any unrelated authorization work. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Locate source-confirmed frontend guards, API guards, and the role helper or middleware path. &lt;!-- authority: llm-explicit --&gt;</li><li>Apply super_admin-to-admin equivalence only where admin access is already source-confirmed. &lt;!-- authority: human-confirmed --&gt;</li><li>Verify admin and super_admin parity and unchanged denial behavior for unrelated roles. &lt;!-- authority: human-confirmed --&gt;</li><li>Record a security or business conflict if recovered source proves distinct super_admin semantics. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Add roles, permissions, role-assignment UI, supplier portal scope, or policy redesign. &lt;!-- authority: llm-explicit --&gt;</li><li>Change login redirects, token handling, cookie handling, public routes, or deployment configuration. &lt;!-- authority: llm-explicit --&gt;</li><li>Run the application locally, commit credentials, or deploy without operator instruction. &lt;!-- authority: human-edited --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-DISCOVERY-001</code> must identify the application path, guard implementations, and role representation, or record each unavailable, before implementation starts. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Security review remains required before implementation because this task changes authorization behavior. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex assisted with converting handoff documentation and approved defaults into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task does not invent a role, permission matrix, redirect, policy, or authentication contract that the recovered source does not establish. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: An operator must review the task before implementation begins. &lt;!-- authority: human-edited --&gt;</li></ul>";
