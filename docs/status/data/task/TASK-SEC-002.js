window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SEC-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Mutating /api routes require same-origin Origin/Referer (webhooks/cron/health excluded). writeVendorOffer requires vendor/admin. Admin AI baseUrl must be on the allowlist; Production chat is 410 unless AI_CHAT_ENABLED=1. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p>The 2026-08-13 audit recorded this gap against the live greenfield app. Existing REBUILD tasks stay done; this slice supersedes the matching on_hold row where noted. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Implement the slice in <code>app/web</code> with Postgres transactions, HTTP handlers that return JSON error envelopes, and tests that import handlers or cores instead of grepping route source. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: src/proxy.ts matcher; POST /api/vendor/offers; /api/admin/ai-settings and /api/admin/ai/chat. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Reopen TASK-REBUILD-001…023. Rejected — those tasks are done; new IDs carry the audit work. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Invent royalty, tax, shipping, live payment keys, Zalo OA, or WordPress DNS. Rejected — those wait on named DEC records. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the audit finding is open. Target: the routes and tests named below are implemented and <code>npm test</code> covers them. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: no Production deploy, merge, or <code>seed:local</code> against Production from this task; no <code>sk_live_</code> / <code>PAYPAL_MODE=live</code>. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Mutating /api routes require same-origin Origin/Referer (webhooks/cron/health excluded). writeVendorOffer requires vendor/admin. Admin AI baseUrl must be on the allowlist; Production chat is 410 unless AI_CHAT_ENABLED=1. &lt;!-- authority: llm-explicit --&gt;</li><li>Tests: tests/catalog-core.test.mjs customer offer rejection; tests/ai-settings-core.test.mjs allowlist. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>WordPress DNS, live Stripe/PayPal, invented finance numbers, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-REBUILD-023</code> (done). HITL remains required at reviewing → ready_to_test and testing → done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor cloud agent implementing the approved audit plan. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
