window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-022"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Establish the greenfield <code>app/web</code> quality-gate and preview-release process: a documented, scriptable pre-review checklist (lint, test, verify, build, CyberOS gates), CapRover preview packaging validation that runs without live hosting credentials, and explicit refusal of production deploy or push from this task. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>Handoff conventions require code → self-review (typecheck/lint/tests) → private GitHub → CapRover preview → verify on the real URL, and never treat a local long-running server as acceptance. Task 1 already added lint/test/verify/build entry points plus Docker and CapRover packaging, but the greenfield app still lacks a single quality-and-preview-release process module that (a) aggregates the required checks, (b) validates preview packaging offline, and (c) records when live CapRover credentials are absent without failing the task. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Standing orders forbid push, deploy, and merge in this session. CapRover or preview hosting credentials may be missing. The task must therefore ship a process that completes on local/CI evidence and must not require a live preview deploy to reach <code>done</code>. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a <code>quality-preview-release-core</code> module (or equivalent) in <code>app/web</code> that:</p>\n<ol><li>Defines a closed quality checklist for greenfield <code>app/web</code>: <code>lint</code>, <code>test</code>, <code>verify</code>, <code>build</code>, and the repo CyberOS gate command <code>bash .cyberos/cuo/gates/run-gates.sh</code>. Document the same checklist in <code>OPERATIONS.md</code> (or equivalent) as the pre-review and pre-preview-release bar. &lt;!-- authority: human-confirmed --&gt;</li><li>Defines a preview-release preparation mode that validates <code>Dockerfile</code>, <code>captain-definition</code> (schemaVersion 2), standalone Next.js packaging assumptions, and secret-handling constraints without contacting CapRover, GitHub, or any remote host. &lt;!-- authority: llm-explicit --&gt;</li><li>Provides a prepare/dry-run script that exits successfully when packaging and quality contracts pass locally, and records a structured outcome of <code>prepared_local</code> when live CapRover credentials are absent — never inventing tokens, never writing secrets, and never calling a deploy API. &lt;!-- authority: human-confirmed --&gt;</li><li>Explicitly refuses production deploy and unauthorized push/merge: any prepare path that targets production or attempts remote publish without an explicit operator-authorized credential path MUST fail closed with a clear error class (for example <code>refused_production</code> / <code>missing_preview_credentials</code>). &lt;!-- authority: human-confirmed --&gt;</li><li>Adds core tests and a verify script proving checklist completeness, offline package validation, credential-absent success path, production-refusal path, and absence of network deploy calls in the default path. Wire the verify script into <code>npm run verify</code>. &lt;!-- authority: llm-explicit --&gt;</li></ol>\n<p>Leave Tasks 1–21 cores, wordpress-import, and non-rebuild <code>on_hold</code> work intact. Do not push, deploy, merge, or add CapRover/GitHub credentials to the repository. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Require a live CapRover preview deploy and real <code>*.server.sachviet.us</code> browser check before this task can finish. This is rejected because standing orders forbid deploy in this session and credentials may be absent; the mission authorizes local/CI process completion without live hosting credentials. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Add GitHub Actions that push or deploy on every green build. This is rejected because private-repo CI may be desirable later, but automatic remote publish contradicts the no-push/no-deploy standing order and is not required to establish the process. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Treat Task 1's existing OPERATIONS notes as sufficient and skip a verifiable process module. This is rejected because Task 23 (parity/cutover planning) depends on a named quality and preview-release bar with testable outcomes, not prose alone. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Reuse or revive <code>app/misc/deploy.sh</code> from the inherited Nuxt/Laravel stack. This is rejected under greenfield-only discipline; the greenfield process must live under <code>app/web</code> against Next.js packaging. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: greenfield has foundation quality commands and CapRover packaging files, but no dedicated quality-and-preview-release process module with offline prepare outcomes and production refusal. Target: tests/verify prove the checklist is documented and enforced, offline package validation passes, credential-absent prepare returns <code>prepared_local</code>, production/unauthorized remote paths refuse, and <code>npm run verify</code> includes the new verifier. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: standing orders forbid push/deploy/merge; secrets must stay out of git; prior cores stay intact. Target: inspection/tests prove no CapRover/GitHub credential files added, no default-path network deploy, no mutation of Tasks 1–21 product cores beyond the minimal OPERATIONS/package.json verify wiring needed here, and no production deploy script that runs without explicit operator authorization. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes the greenfield quality-gate and preview-release process for <code>app/web</code>. It does not perform a live preview deploy, production release, or remote push. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Quality checklist core + OPERATIONS documentation for lint, test, verify, build, and CyberOS gates. &lt;!-- authority: human-confirmed --&gt;</li><li>Offline CapRover/Docker preview packaging validation (captain-definition schemaVersion 2, Dockerfile, standalone constraints, secret rules). &lt;!-- authority: llm-explicit --&gt;</li><li>Prepare/dry-run script with <code>prepared_local</code> when live credentials are absent. &lt;!-- authority: human-confirmed --&gt;</li><li>Closed refusal of production deploy and unauthorized remote publish from the default path. &lt;!-- authority: human-confirmed --&gt;</li><li>Core/route-free tests and verify script wired into <code>npm run verify</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Live CapRover deploy, CapRover dashboard login, or browser acceptance on <code>*.server.sachviet.us</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Production promotion, DNS cutover, Cloudflare changes, or WordPress retirement. &lt;!-- authority: human-confirmed --&gt;</li><li>Pushing to GitHub, opening PRs, merging, or storing CapRover/GitHub tokens in the repo. &lt;!-- authority: human-confirmed --&gt;</li><li>Reopening or implementing non-rebuild <code>on_hold</code> quality/cutover/migration tasks. &lt;!-- authority: human-confirmed --&gt;</li><li>Mutating wordpress-import or Tasks 1–21 product cores beyond minimal verify/OPERATIONS wiring. &lt;!-- authority: human-confirmed --&gt;</li><li>Inventing a full GitHub Actions CI matrix as a mandatory deliverable (documenting the local/CI command bar is enough). &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Tasks 1–3 provide the Next.js foundation, identity/session baseline, and shared web foundations that quality commands already exercise. Later Task 23 depends on this process as the quality and preview-release bar before parity/cutover planning. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented quality/preview conventions and operator standing orders into this rebuild process task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task establishes offline quality and preview-release process artefacts for <code>app/web</code>; it excludes live deploy, push, merge, and credential invention. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue, session-wide routine acceptance gates, and standing orders to never push/deploy/merge and to complete without live CapRover credentials when absent. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-022.</em></p>";
