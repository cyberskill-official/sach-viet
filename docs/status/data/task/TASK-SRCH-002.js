window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SRCH-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Replace unbounded <code>listPublicProducts</code> plus in-process rank with Postgres <code>tsvector</code> / <code>pg_trgm</code> in an additive migration (<code>PKG-20</code>, <code>FL-B2C-03</code>). Keep the Meilisearch seam optional/off. Search must not load all products to rank. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p><code>app/web/src/lib/vietnamese-search-core.mjs</code> calls <code>listPublicProducts</code> without a bound and ranks in process. <code>FL-B2C-03</code> requires indexed Postgres search that recovers from timeout without loading all products. TASK-SEARCH-001 is closed (Meilisearch gated); TASK-REBUILD-020 stays done. This new ID carries the Postgres FTS/trigram slice. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Additive migration for <code>tsvector</code> and <code>pg_trgm</code> indexes on public catalog text. Query in the database with a bounded limit and cursor; normalize Vietnamese input; return ranked pages and bounded suggestions. Do not hydrate the full catalog then rank. Meilisearch remains optional/off — do not stand up a Meilisearch service. New/changed search handlers follow TASK-API-001 envelope/cursor when that task has landed, or match its contract if implemented together. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: existing public catalog/search routes; no new privileged search surface. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Stand up Meilisearch now. Rejected — TASK-SEARCH-001 stays closed; golive wave keeps Meilisearch optional/off. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Reopen TASK-REBUILD-020. Rejected — that task is done; this ID carries the leftover. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: search hydrates all then ranks. Target: bounded Postgres FTS/trigram query; tests prove it does not load the full catalog; <code>npm test</code> covers the named tests. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: additive migration only; no Production deploy or merge from this task; no Meilisearch requirement. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Additive <code>tsvector</code> / <code>pg_trgm</code> migration and query path in <code>vietnamese-search-core.mjs</code> / catalog search. &lt;!-- authority: llm-explicit --&gt;</li><li>Bounded suggestions and ranked pages; timeout recovery without hydrate-all. &lt;!-- authority: llm-explicit --&gt;</li><li>Meilisearch seam stays optional/off. &lt;!-- authority: llm-explicit --&gt;</li><li>Tests: core/handler tests that a catalog larger than the page size is not fully loaded to rank (for example extend or add <code>tests/vietnamese-search-core.test.mjs</code>); no source-grep route tests. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Meilisearch cluster, US-region move, invented ranking SLAs, live keys, WordPress DNS, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-DATA-001</code> and <code>TASK-UI-001</code> are the in-flight audit set (async pg + storefront/search wiring). This task is eligible only after those complete HITL (<code>testing → done</code>). HITL remains required at reviewing → ready_to_test and testing → done. Do not request HITL while authoring. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor agent authoring the local-complete golive wave queue. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates after implementation. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
