window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-JOB-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Register, verify, and password-reset must enqueue identity mail on the leased outbox (<code>identity.verify</code> / <code>identity.reset</code>) instead of succeeding with no durable send. Production without SMTP stays <code>failed</code>, not <code>delivered</code>. Tests inject <code>submit</code>. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p>The local-complete golive wave records that register/verify/reset currently succeed without writing the outbox. TASK-JOB-001 ships the leased worker and real submitter; TASK-ID-001 ships the identity routes. Until they are hooked together, verification and reset mail never leave the worker path (<code>FL-ID-01</code>, <code>FL-ID-06</code>, <code>FL-PLT-05</code> transactional slice). &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Hook <code>app/web/src/lib/auth-core.mjs</code> to <code>app/web/src/lib/order-comms-outbox-core.mjs</code> (or a shared <code>jobs</code> table using the same lease) with kinds <code>identity.verify</code> and <code>identity.reset</code>. Drain remains <code>GET /api/cron/drain-order-comms</code> with <code>CRON_SECRET</code>. Mark <code>delivered</code> only after a real submitter send. Without SMTP/Resend config, rows stay <code>failed</code>, not <code>delivered</code>. Tests inject <code>submit</code>. Do not invent sender domain, SPF/DKIM, or Zalo OA policy (DEC-COMMS-001). &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: existing <code>POST /api/auth/register</code>, <code>GET|POST /api/auth/verify</code>, <code>POST /api/auth/forgot</code>, <code>POST /api/auth/reset</code>; cron drain unchanged. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Send identity mail inline from the HTTP handler. Rejected — TASK-JOB-001 already requires leased outbox + real submitter. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Activate Zalo OA or invent a production sender domain. Rejected — DEC-COMMS-001. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: identity routes succeed with no outbox row. Target: register/verify/reset enqueue the named kinds; missing SMTP → <code>failed</code> not <code>delivered</code>; <code>npm test</code> covers injected <code>submit</code>. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: no Production deploy or merge from this task; no secret values in logs; no <code>sk_live_</code> / <code>PAYPAL_MODE=live</code>. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Enqueue <code>identity.verify</code> / <code>identity.reset</code> from auth-core onto the leased outbox. &lt;!-- authority: llm-explicit --&gt;</li><li>Production without SMTP stays <code>failed</code>, not <code>delivered</code>. &lt;!-- authority: llm-explicit --&gt;</li><li>Tests inject <code>submit</code>: extend <code>tests/auth-core.test.mjs</code> / <code>tests/auth-http.test.mjs</code> and <code>tests/order-comms-outbox-core.test.mjs</code> (handler/core, not greps). &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Signed email provider/domain/Zalo OA (DEC-COMMS-001), marketing consent, live payment keys, WordPress DNS, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-JOB-001</code> and <code>TASK-ID-001</code> are the in-flight audit set. This task is eligible only after those complete HITL (<code>testing → done</code>). HITL remains required at reviewing → ready_to_test and testing → done. Do not request HITL while authoring. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor agent authoring the local-complete golive wave queue. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates after implementation. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
