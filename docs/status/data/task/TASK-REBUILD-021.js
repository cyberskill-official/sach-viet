window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-021"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build greenfield WordPress import compatibility in <code>app/web</code>: a fixture-driven importer that maps WordPress/WooCommerce-shaped user and order payloads into Task 2 identity and Task 5 commerce stores, preserves legacy identifiers, matches orders by billing email plus total amount, verifies imported PHPass password hashes without reviving WordPress as the product runtime, and records dry-run or apply outcomes without connecting to a live WordPress MySQL. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The handoff documents a one-way WordPress → platform data flow (<code>WpImport</code>), WP-imported rows that carry legacy identifiers, order matching by <code>(billing_email→user_id, total_amount)</code>, and PHPass compatibility so imported accounts keep their passwords. The greenfield Next.js app has Task 2 scrypt auth and Task 5 orders, but no import adapter, no legacy-id columns, no PHPass verify path, and no documented order-match gate. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>The greenfield-only decision forbids implementing against unavailable legacy Laravel/<code>WpImport</code> source or treating WordPress as the product application. Non-rebuild <code>TASK-MIGRATION-001</code> (324 unmatched items) and cutover tasks stay on hold and must not be reopened. Connecting CI to a live WP MySQL or shipping a WordPress PHP runtime would violate greenfield constraints and invent production data access the sources do not authorize for this rebuild slice. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a <code>wordpress-import-core</code> module (or equivalent) in <code>app/web</code> that:</p>\n<ol><li>Defines a closed WordPress/WooCommerce-shaped fixture schema for users (email, PHPass hash, display name, legacy user id) and orders (billing email, total amount, line items, legacy order/item ids, currency). Fixtures are JSON documents checked into tests or generated in-memory — never a live MySQL dump requirement for CI. &lt;!-- authority: human-confirmed --&gt;</li><li>Imports users into the Task 2 auth store with preserved <code>legacy_wp_user_id</code> (or equivalent), storing the PHPass hash as the password credential for imported accounts. New greenfield accounts continue to use scrypt; imported PHPass hashes are accepted only when the stored hash matches the portable <code>$P$</code> / <code>$H$</code> PHPass form. &lt;!-- authority: llm-explicit --&gt;</li><li>Extends password verification so <code>login</code> accepts either scrypt (native) or PHPass (imported) hashes without inventing plaintext passwords or logging hash material. &lt;!-- authority: human-confirmed --&gt;</li><li>Imports orders into the Task 5 commerce store with preserved <code>legacy_wp_order_id</code> / <code>legacy_wp_order_item_id</code>, resolving the customer by normalized billing email → user id and matching on total amount as the documented gate. Unmatched orders MUST be recorded as <code>unmatched</code> outcomes without inventing slug-heuristic rematches. &lt;!-- authority: llm-explicit --&gt;</li><li>Supports <code>dry_run</code> and <code>apply</code> modes. Both append import-run / import-item outcome rows (accepted, skipped_duplicate, unmatched, rejected). <code>dry_run</code> MUST NOT mutate users or orders. <code>apply</code> MUST be idempotent on legacy ids (re-import of the same legacy id skips or no-ops). &lt;!-- authority: human-confirmed --&gt;</li><li>Exposes an admin-only import status/read and optional apply endpoint gated by Task 2 admin role, accepting fixture payloads in tests; default CI never opens a network socket or MySQL connection. &lt;!-- authority: llm-explicit --&gt;</li></ol>\n<p>Leave Task 2/5 ownership intact aside from the minimal schema and verifyPassword extensions required here. Leave <code>TASK-MIGRATION-001</code>, <code>TASK-CUTOVER-001</code>, <code>TASK-CUTOVER-002</code>, Vietnamese search, email/Zalo, and prior cores unchanged for ownership. Do not revive WordPress, Dokan, WooCommerce admin, or Laravel <code>WpImport</code> as the product app. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Recover and run the unavailable Laravel <code>WpImport</code> command against live WordPress MySQL. This is rejected under the greenfield-only decision: legacy code is unavailable, and live revenue DB access is out of band for this rebuild slice. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Embed a WordPress PHP runtime or WooCommerce admin inside <code>app/web</code> as a compatibility shim. This is rejected because the product app is the greenfield Next.js platform; import compatibility is a data adapter, not a hybrid frankenstein runtime. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Reopen <code>TASK-MIGRATION-001</code> to reconcile the reported 324 unmatched items, or advance cutover tasks. This is rejected by standing orders to leave non-rebuild <code>on_hold</code> work alone; this task only builds the greenfield import/compat layer and records unmatched outcomes for later. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Bulk-match unmatched orders by Vietnamese slug heuristics. This is rejected because migration doctrine forbids inventing matching rules without source evidence, and incorrect matches corrupt imported data. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Require production WordPress credentials or CapRover <code>sachviet-current-db</code> connectivity before CI can pass. This is rejected because greenfield CI must stay credential-free and fixture-driven. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: greenfield has scrypt-only auth and commerce orders with no legacy ids, no PHPass verify, and no WP-shaped import path. Target: tests prove fixture users import with legacy ids and PHPass login success, fixture orders import when billing email + total match, dry_run mutates nothing, apply is idempotent on legacy ids, unmatched orders are recorded without slug heuristics, and admin-only status/apply refuses non-admin actors. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: greenfield-only forbids legacy runtime revival and live WP DB dependence; migration/cutover stay on hold. Target: tests/verify prove no WordPress PHP runtime, no MySQL client connection in the import core default path, no mutation of <code>TASK-MIGRATION-001</code> / cutover specs, and Task 2/5 native scrypt + checkout paths remain intact for non-imported accounts. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes fixture-driven WordPress/WooCommerce import compatibility and PHPass verify support in the greenfield app. It does not reconnect live WordPress, reopen migration/cutover tasks, or invent unmatched-item reconciliation rules. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Closed WP/Woo-shaped fixture schema for users and orders. &lt;!-- authority: human-confirmed --&gt;</li><li>User import with <code>legacy_wp_user_id</code> and PHPass hash storage; login verify for <code>$P$</code>/<code>$H$</code> hashes. &lt;!-- authority: human-confirmed --&gt;</li><li>Order import with legacy order/item ids, billing-email → user resolution, and total-amount match gate. &lt;!-- authority: llm-explicit --&gt;</li><li><code>dry_run</code> / <code>apply</code> modes with append-only import outcome records and idempotent re-import. &lt;!-- authority: human-confirmed --&gt;</li><li>Admin-gated import status/apply API (or equivalent) for fixture payloads. &lt;!-- authority: llm-explicit --&gt;</li><li>Core/route tests and a verify script proving fixture import, PHPass login, match/unmatched paths, dry_run safety, idempotency, and absence of WP runtime / MySQL client defaults. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Live WordPress MySQL connectivity, CapRover <code>sachviet-current</code> / <code>sachviet-current-db</code> provisioning, or production re-import. &lt;!-- authority: human-confirmed --&gt;</li><li>WordPress PHP runtime, WooCommerce/Dokan admin, Elementor, or Laravel <code>WpImport</code> command recovery. &lt;!-- authority: human-confirmed --&gt;</li><li>Reopen or implement <code>TASK-MIGRATION-001</code>, <code>TASK-CUTOVER-001</code>, or <code>TASK-CUTOVER-002</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Slug-heuristic rematching of unmatched order items, DNS cutover, or WordPress retirement. &lt;!-- authority: llm-explicit --&gt;</li><li>Catalog media CDN migration, full WooCommerce product catalog dump import, or vendor Dokan payout history import beyond user/order fixtures needed for auth+order compatibility. &lt;!-- authority: llm-explicit --&gt;</li><li>Mutate Vietnamese search, email/Zalo, notification/SSE, or publisher/author cores for ownership. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 2 provides signed sessions, roles, and the users table this task extends with legacy ids and PHPass verify. Task 5 provides the orders / order_items commerce store this task extends with legacy ids and imported paid/historical order rows. Later cutover/parity work may consume import outcomes but is out of scope here. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented WpImport / PHPass / legacy-identifier handoff and greenfield-only constraints into this rebuild task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task builds fixture-driven import compatibility and PHPass verify in <code>app/web</code>; it excludes live WordPress runtime revival, MySQL dependence, and on-hold migration/cutover work. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue, session-wide routine acceptance gates, and standing orders to leave non-rebuild on_hold tasks alone and to pause only for real decisions. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-021.</em></p>";
