window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-010"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield in-app notification inbox, unread badge count, deeplink targets, notification event-type registry, and per-user plus per-vendor preference/channel records under signed sessions in <code>app/web</code>. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The greenfield application has identity and portal shells but no shared notification surface. The source identifies notifications as a cross-cutting system for all portals (in-app bell plus preferences), with data-model records for <code>Notification</code>, <code>NotificationEventType</code> (10+ trigger keys), <code>UserNotificationPreference</code>, <code>VendorNotificationPreference</code>, and <code>UserChannel</code>. Real-time delivery and external email/Zalo channels remain separate later work. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite notification repository and signed-session route handlers in <code>app/web</code>. Seed a closed event-type registry of at least ten trigger keys grounded in existing greenfield domains (<code>order.paid</code>, <code>order.payment_failed</code>, <code>support.ticket_created</code>, <code>support.ticket_message</code>, <code>goods_request.created</code>, <code>product_review.created</code>, <code>vendor.application_submitted</code>, <code>vendor.application_decided</code>, <code>vendor.offer_written</code>, <code>payout.created</code>, <code>payout.status_changed</code>). Persist user-owned inbox notifications with title, body, event type, unread state, and a portal-relative deeplink path. Expose unread badge counts and mark-read for the signed-in owner only. Persist user and vendor notification preferences that gate in-app delivery per event type, plus <code>UserChannel</code> records limited to the <code>in_app</code> channel in this task. Emit safe structured events that omit session tokens, email addresses, request bodies, and payment secrets. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Add WebSocket, SSE, Reverb, or Pusher live delivery now. This is rejected because the source and rebuild sequence reserve real-time notifications for Task 11. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Send email, Zalo, SMS, or other external channels from preference toggles. This is deferred because integration credentials and delivery policy belong to Task 19. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Recover or verify a legacy Laravel/Nuxt notification backend. This is rejected under the greenfield-only rebuild decision. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the greenfield application has no notification inbox, badge count, event-type registry, or preference/channel persistence. Target: tests prove a signed-in user can receive preference-gated in-app notifications, read an unread badge count, mark owned notifications read, update own preferences and the <code>in_app</code> channel, a vendor can update vendor preferences, and unauthorized actors cannot read or mutate another user's notification records. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: live transport and external messaging are later tasks, and secrets must stay out of notification payloads and events. Target: tests prove the implementation creates no WebSocket/SSE/Reverb/Pusher transport, no email/Zalo/SMS send path, and omits session tokens, email addresses, request bodies, and payment secrets from responses and structured events. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes the in-app notification inbox, badge, deeplink fields, event-type registry, and preference/channel persistence under signed sessions. It does not invent live delivery, external messaging, or deployment. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Closed <code>notification_event_types</code> registry with at least ten source-grounded trigger keys. &lt;!-- authority: human-confirmed --&gt;</li><li>User-owned <code>notifications</code> inbox with unread state and portal-relative deeplink path. &lt;!-- authority: human-confirmed --&gt;</li><li>Unread badge count and mark-read for the signed-in owner only. &lt;!-- authority: human-confirmed --&gt;</li><li><code>user_notification_preferences</code> and <code>vendor_notification_preferences</code> that gate in-app creation per event type. &lt;!-- authority: human-confirmed --&gt;</li><li><code>user_channels</code> limited to the <code>in_app</code> channel for this task. &lt;!-- authority: llm-explicit --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, and payment secrets. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Live notification transport (WebSocket, SSE, Reverb, Pusher, or polling push). &lt;!-- authority: human-confirmed --&gt;</li><li>Email, Zalo, SMS, push-provider credentials, template rendering, or SMTP delivery. &lt;!-- authority: human-confirmed --&gt;</li><li>Legacy WordPress/Laravel/Nuxt recovery, multi-tenant fan-out beyond owner/vendor preference gates, or deployment. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 2 provides signed sessions and role normalization. Task 3 provides shared portal foundations used by deeplink paths and portal shells. Later Task 11 may add live delivery on top of this inbox. Later Task 19 may add email and Zalo channels after credentials exist. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented cross-cutting notification gaps and greenfield rebuild sequence into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task excludes live transport, external messaging credentials, and legacy recovery. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue and session-wide routine acceptance gates. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
