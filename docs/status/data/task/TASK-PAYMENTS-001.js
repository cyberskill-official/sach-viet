window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PAYMENTS-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Wire Stripe <strong>test-mode</strong> hosted Checkout on Vercel Production and implement PayPal Orders <strong>sandbox</strong> as a second checkout provider. Use only sandbox/test credentials from the private handoff. Never commit secrets or live keys. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>Greenfield Stripe Checkout + webhook → <code>paid</code> already exists, but Production omits <code>STRIPE_*</code> (Wave 4 item 6 deferred). PayPal is net-new (<code>paypal_checkout</code> deferred). Operators need a proven sandbox paid path on <code>https://sachviet.cyberskill.world</code> without live money. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<ol><li>Extend <code>POST /api/checkout</code> with <code>provider: \"stripe\" | \"paypal\"</code> (default <code>stripe</code>).</li><li>Add migration <code>003_payment_provider.sql</code> (<code>payment_provider</code>, <code>paypal_order_id</code>).</li><li>Implement PayPal OAuth + Orders create/capture + return route + webhook with fetch doubles in tests.</li><li>Cart UI: two CTAs (Stripe / PayPal).</li><li>Upsert Production sandbox/test env; register webhooks; redeploy; prove paid path.</li><li>Refuse <code>sk_live_</code> / <code>PAYPAL_MODE=live</code> in wire helpers where easy. &lt;!-- authority: human-confirmed --&gt;</li></ol>\n<h2>Alternatives Considered</h2>\n<p>Ship live keys on Production. Rejected — operator unlock is sandbox/test only. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>PayPal only without Stripe wire. Rejected — Stripe code exists and handoff provides verified test keys. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: Production checkout fails closed without Stripe; PayPal absent. Target: Stripe test card and PayPal sandbox buyer can reach <code>paid</code> on Production (or code+wire complete with exact operator residual if registration blocks). Deadline: before testing → done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: no payment secrets in git. Target: env names only in docs; no <code>sk_live_</code> / live PayPal mode accepted by helpers. Deadline: before testing → done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Checkout provider branching; Stripe Production test env + webhook secret registration.</li><li>PayPal sandbox create/capture/return/webhook; cart dual CTAs; migration 003.</li><li>Docs/env examples; ops Stripe row → done (sandbox); parity <code>paypal_checkout</code> when proven.</li><li>Focused tests + gates; feature-branch commit + PR (no merge without operator). &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Live Stripe/PayPal keys or <code>PAYPAL_MODE=live</code>.</li><li>Legacy Laravel/Nuxt payment code; coupons; Phase B/C; WP; royalty unlock.</li><li>Self-setting task <code>done</code> (HITL required). &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-REBUILD-005</code> (cart/checkout/Stripe core). Private handoff for sandbox values (never committed). &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Composer assisted authoring from the approved sandbox payments plan. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: No live keys or handoff secret values written into the repository. &lt;!-- authority: human-confirmed --&gt;</li><li>Human review: Operator HITL at reviewing → ready_to_test and testing → done. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
