window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Create the empty SachViet application workspace as a greenfield Nuxt 4 frontend and Laravel 10 API foundation. The workspace must support later portal work without reusing the inherited implementation. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The repository contains the handoff documents and CyberOS workflow assets, but no SachViet application source. The operator has confirmed that the inherited system is to be rebuilt rather than incrementally improved. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>The handoff describes the expected new-platform boundary as <code>app/web</code> for Nuxt and <code>app/api</code> for Laravel, with separate packaging for CapRover preview. It also defines private repositories, secret isolation, and preview-based verification as project constraints. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Create an <code>app/</code> workspace with a Nuxt 4, Vue 3, TypeScript frontend in <code>app/web</code> and a Laravel 10, PHP 8.1 API in <code>app/api</code>. Establish only the source-confirmed project structure and quality-command baseline needed for the following rebuild tasks. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Provide Docker and CapRover packaging surfaces consistent with the documented preview deployment model, without deploying or adding credentials. Keep application secrets outside version control and preserve the documented rule that browser verification occurs on preview rather than through a locally running application. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Attempt to locate and adapt the inherited application source. This was rejected because the operator explicitly chose a rebuild from scratch. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Choose a different frontend or API stack. This was deferred because the approved recommended defaults retain the documented Nuxt and Laravel direction, while a stack replacement would be a separate architecture decision. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: this repository has no SachViet application source. Target: the repository contains source-controlled <code>app/web</code> and <code>app/api</code> foundations matching the documented technology direction, with repeatable build, lint, and test commands. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: project conventions prohibit public repositories, committed secrets, and local application runtime verification. Target: the foundation does not include secrets, does not change repository visibility, and documents preview-only application verification. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>The task creates the greenfield repository foundation and quality-command entry points. It does not implement any business portal, database domain, payment flow, or external integration. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Create the <code>app/web</code> and <code>app/api</code> source roots described in the architecture handoff. &lt;!-- authority: llm-explicit --&gt;</li><li>Add the source-confirmed Nuxt, Laravel, Docker, and CapRover packaging foundations without credentials. &lt;!-- authority: llm-explicit --&gt;</li><li>Add repeatable build, lint, and test command entry points for the new source roots. &lt;!-- authority: human-confirmed --&gt;</li><li>Record preview-only verification and secret-handling constraints near the application workspace. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Reuse, copy, or patch the inherited application implementation. &lt;!-- authority: human-confirmed --&gt;</li><li>Implement authentication, database entities, portal pages, payments, integrations, or migrations. &lt;!-- authority: llm-explicit --&gt;</li><li>Push, deploy, promote, add credentials, or alter legacy production data. &lt;!-- authority: human-edited --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>This is the first greenfield task and has no application-task dependency. It unblocks all subsequent rebuild tasks through its source roots and quality-command baseline. &lt;!-- authority: llm-implicit --&gt;</p>\n<p>The documented private-repository, secret-isolation, and preview-verification rules remain binding. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex converted the approved handoff documentation and operator rebuild decision into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task does not invent endpoint contracts, database fields, credentials, or deployment access. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the task-author plan before task drafting began. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
