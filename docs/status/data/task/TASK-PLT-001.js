window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLT-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Add <code>GET /api/ready</code> that pings the database, reports latest migration id, outbox oldest-age, and required env <em>presence</em> without secret values. <code>GET /api/health</code> stays process/liveness only (<code>FL-PLT-01</code>). Extend local <code>smoke:docker</code> to hit <code>/api/ready</code>. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p>The local-complete golive wave and <code>FL-PLT-01</code> require a readiness probe distinct from liveness. <code>app/web/src/app/api/health/route.ts</code> exists; <code>/api/ready</code> does not. Load balancers that treat health as ready can route traffic before Postgres, migrations, or outbox config are usable. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add <code>app/web/src/app/api/ready/route.ts</code>. Readiness checks: DB ping, latest migration id, outbox oldest-age, required env presence (names only; never values). Do not expose error internals or secrets. Health remains process-only. Tests import the ready handler; do not add source-grep route tests. US-region move, PITR drill, and staging Vercel stay in DEC-OPS-001 / PKG-03 and are out of this task. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: <code>GET /api/health</code> (unchanged liveness); <code>GET /api/ready</code> (readiness; unauthenticated probe, no secrets). &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Fold readiness into <code>/api/health</code>. Rejected — <code>FL-PLT-01</code> requires liveness to check the process only. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Implement US-region Supabase, PITR, or a staging Vercel project in this task. Rejected — DEC-OPS-001 / PKG-03, not this wave. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: only <code>/api/health</code> exists. Target: <code>/api/ready</code> returns readiness JSON without secrets; health stays liveness; <code>smoke:docker</code> hits ready; <code>npm test</code> covers the named tests. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: no Production deploy, merge, or secret values in probe bodies; no <code>sk_live_</code> / <code>PAYPAL_MODE=live</code>. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li><code>GET /api/ready</code>: DB ping, latest migration id, outbox oldest-age, required env presence. &lt;!-- authority: llm-explicit --&gt;</li><li><code>GET /api/health</code> stays process/liveness. &lt;!-- authority: llm-explicit --&gt;</li><li>Local <code>smoke:docker</code> hits <code>/api/ready</code>. &lt;!-- authority: llm-explicit --&gt;</li><li>Tests: handler-imported ready/health assertions (for example <code>tests/ready-http.test.mjs</code>); no <code>*-route.test.mjs</code> source greps. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>US-region cutover, PITR drill, staging Vercel project (DEC-OPS-001), live keys, WordPress DNS, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-DATA-001</code> and <code>TASK-JOB-001</code> are the in-flight audit set (async pg + leased outbox age). This task is eligible only after those complete HITL (<code>testing → done</code>). HITL remains required at reviewing → ready_to_test and testing → done. Do not request HITL while authoring. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor agent authoring the local-complete golive wave queue. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates after implementation. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
