window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-014"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield B2B quote → order conversion and private contract/purchase-order artifact association in <code>app/web</code>, on top of the existing won-quote pipeline and commerce money conventions, while keeping brokerage blind and leaving the quote-pipeline module intact. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The vision describes PO-based institutional ordering after B2B staff quote and negotiate. The <code>/b2b</code> portal still needs quote → order conversion and contract/PO artifacts after the quote pipeline ships. The roadmap lists quote → order conversion as the next phase. Task 13 deliberately excluded order creation, contracts, and POs. The greenfield app has won quotes and B2C commerce orders, but no institutional order or artifact domain. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite B2B order repository and signed-session route handlers in <code>app/web</code> that consume Task 13 quote records without rewriting the quote-pipeline module. Persist <code>b2b_orders</code> linked one-to-one to a <code>won</code> quote, copying organization ownership and quote line items into <code>b2b_order_items</code> with required non-negative USD unit prices using the same money conventions as commerce checkout. Persist private <code>b2b_artifacts</code> rows of kind <code>contract</code> or <code>purchase_order</code> with a staff-supplied reference number and an opaque private storage key — not a public URL. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Allow <code>employee_b2b</code> and <code>admin</code> actors to convert only a <code>won</code> quote into an order when every quote line has a unit price, attach contract/PO artifacts to that order, list and read staff order detail, and advance order status only along the closed transitions <code>awaiting_po→confirmed</code> (only when at least one <code>purchase_order</code> artifact exists) and <code>awaiting_po→cancelled</code>. Allow <code>school_librarian</code> and <code>admin</code> actors to list and read organization-owned orders and artifact reference numbers without receiving storage keys, vendor ids, supplier identifiers, payout facts, or Stripe checkout fields. Do not create B2C cart/checkout orders from quotes, do not call Stripe, and do not mutate quote status transitions or quote schema ownership in <code>b2b-quote-core</code>. Emit safe structured events that omit session tokens, email addresses, request bodies, payment secrets, and storage keys. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Reuse B2C <code>orders</code> / Stripe hosted checkout for institutional conversion. This is rejected because the source describes PO-based brokerage ordering, not card checkout, and B2C order items require vendor offers that would break blindness. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Let institution buyers submit POs, track budgets, or receive MARC files in this task. This is deferred to the institution buyer portal work (Task 15). &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Build public document URLs, e-signature, templates, OCR, legal retention, or a new file-storage service. This is rejected because the source does not define those systems; this task only associates private artifact metadata. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Recover a legacy Laravel/Nuxt contract/PO implementation. This is rejected under the greenfield-only rebuild decision. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: won quotes cannot become institutional orders and no contract/PO artifact records exist. Target: tests prove B2B staff can convert a priced <code>won</code> quote into an <code>awaiting_po</code> order once, attach <code>contract</code> and <code>purchase_order</code> artifacts, confirm only after a PO artifact exists, and cancel from <code>awaiting_po</code>; institution members can read organization-owned orders and artifact reference numbers; unauthorized roles cannot. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: brokerage is blind; quote pipeline ownership stays in Task 13; B2C Stripe checkout must not absorb institutional conversion. Target: tests prove institution order responses omit vendor/supplier identifiers and storage keys; conversion refuses non-<code>won</code> quotes, unpriced lines, and duplicate conversion; <code>b2b-quote-core</code> is not rewritten to own order/contract/PO tables; responses and structured events omit session tokens, email addresses, request bodies, payment secrets, and storage keys. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes greenfield quote → B2B order conversion and private contract/PO artifact association under signed sessions. It does not invent institution PO submission UX, budgets, MARC delivery, public document hosting, or deployment. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Convert a priced <code>won</code> quote into a <code>b2b_orders</code> record with copied line items and USD money conventions from commerce. &lt;!-- authority: human-confirmed --&gt;</li><li>Enforce one order per quote and refuse conversion when any line lacks a unit price. &lt;!-- authority: human-confirmed --&gt;</li><li>Persist private contract and purchase-order artifact metadata (kind, reference number, opaque storage key) on a B2B order. &lt;!-- authority: human-confirmed --&gt;</li><li>Closed order statuses <code>awaiting_po</code>, <code>confirmed</code>, and <code>cancelled</code> with transitions <code>awaiting_po→confirmed</code> (PO artifact required) and <code>awaiting_po→cancelled</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Staff list/read/convert/attach/transition surfaces for <code>employee_b2b</code> / <code>admin</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Institution list/read of organization-owned orders and artifact reference numbers without supplier disclosure or storage keys for <code>school_librarian</code> / <code>admin</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, payment secrets, and storage keys. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Rewriting or replacing the Task 13 quote pipeline, status machine, selection lists, or organizations module. &lt;!-- authority: human-confirmed --&gt;</li><li>Creating B2C cart orders, Stripe checkout sessions, vendor-offer-backed retail line items, or payout records from quotes. &lt;!-- authority: human-confirmed --&gt;</li><li>Institution budget tracking, institution-initiated PO submission UX, MARC delivery, or publisher/author earnings. &lt;!-- authority: llm-explicit --&gt;</li><li>Public artifact URLs, e-signature, templates, OCR, conversion pipelines, legal terms, retention policy, or a new file-storage service. &lt;!-- authority: human-confirmed --&gt;</li><li>Legacy WordPress/Laravel/Nuxt recovery, live notification changes, email/Zalo, or deployment. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 5 provides commerce money conventions and the B2C order patterns this task must not overload for PO-based brokerage. Task 13 provides organizations, membership, won quotes, priced quote line items, and the intact quote pipeline this conversion reads. Later Task 15 may deepen institution buyer PO submission, budgets, and MARC delivery on top of these orders. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented B2B brokerage, portal, and roadmap sources into this greenfield rebuild task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task builds quote → order conversion and private contract/PO artifact association and excludes institution PO submission UX, budgets, MARC delivery, public document hosting, Stripe institutional checkout, and quote-pipeline rewrites. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue and session-wide routine acceptance gates. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-014.</em></p>";
