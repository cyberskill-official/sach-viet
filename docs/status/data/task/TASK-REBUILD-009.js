window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-009"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield employee and retail operations foundation: a signed-session employee dashboard with counts derived from existing commerce and support records, an employee-editable home-config store for homepage sections, and retail order-queue reads for <code>employee_b2c</code> and <code>admin</code>. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The greenfield application already has identity, portal shells, paid orders, and support queues, but internal staff still lack an employee hub summary, a persisted home-config surface, and a retail order queue. The source identifies the <code>/employee</code> hub with role-filtered access, an approval queue from the dashboard payload, partially mocked employee KPIs, an unwired home-config editor, and a <code>/retail</code> portal whose orders are display-only for B2C ops staff. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite employee-retail operations repository and signed-session route handlers in <code>app/web</code>. Employee-portal roles (<code>employee</code>, <code>employee_b2c</code>, <code>employee_b2b</code>, and <code>admin</code>) may read a dashboard summary derived from existing order, support-ticket, goods-request, and pending vendor-application counts, plus a pending vendor-application approval queue without customer secrets. Employee and admin actors may read and update homepage <code>home_sections</code> configuration records. Retail-portal roles (<code>employee_b2c</code> and <code>admin</code>) may list commerce orders without customer email, session tokens, or payment secrets. Emit safe structured events for home-config writes. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Invent retail order fulfillment transitions, shipment tracking, refunds, or returns workflow. This is rejected because the source identifies those needs but does not define the greenfield fulfillment-state or returns contract, and earlier rebuild tasks already deferred those transitions. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Build a B2B quote pipeline or institution buyer flows inside this task. This is deferred because the approved rebuild sequence reserves those surfaces for later B2B and institution tasks. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Invent new KPI chart series, report exports, or user-management CRUD beyond the existing role record. This is deferred because the source asks for real baseline dashboard facts and a wired home-config editor, not a new analytics or HR product. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the greenfield application has no employee dashboard summary, home-config persistence, or retail staff order queue. Target: tests prove an authorized employee can read dashboard totals and the pending approval queue, an employee or administrator can persist home sections, an <code>employee_b2c</code> or <code>admin</code> actor can list retail orders, and unauthorized roles cannot. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: order payment states are already limited to pending, paid, and payment_failed, and customer secrets must stay out of staff list views. Target: tests prove the implementation creates no fulfillment, refund, returns, or settlement transitions and omits customer email, session tokens, and payment secrets from employee and retail responses and events. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes employee dashboard reads, home-config persistence, and retail order-queue reads under the documented role boundaries. It does not invent fulfillment policy, returns, refunds, B2B quotes, live notifications, or deployment. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Employee dashboard summary counts derived from existing orders, open support tickets, open goods requests, and pending vendor applications. &lt;!-- authority: human-confirmed --&gt;</li><li>Pending vendor-application approval-queue reads for employee-portal roles, without resolving applications outside the existing admin decision path. &lt;!-- authority: llm-explicit --&gt;</li><li>Home-config <code>home_sections</code> persistence with create/update/list for employee and administrator actors. &lt;!-- authority: human-confirmed --&gt;</li><li>Retail order-queue reads for <code>employee_b2c</code> and <code>admin</code>, excluding customer secrets. &lt;!-- authority: human-confirmed --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, and payment secrets. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Order fulfillment states, shipment tracking, refunds, returns workflow, carrier labels, or bulk processing. &lt;!-- authority: human-confirmed --&gt;</li><li>B2B quote pipeline, institution budget/PO flows, publisher/author earnings, or supplier portal work. &lt;!-- authority: llm-explicit --&gt;</li><li>New user-management CRUD, chart-series analytics, report export, legacy WordPress/Laravel/Nuxt recovery, or deployment. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 2 provides signed sessions and role normalization, including employee and retail portal role bundles. Task 3 provides shared portal foundations. Task 5 provides orders used by the employee summary and retail queue. Existing support and admin-commerce tables supply ticket, goods-request, and vendor-application counts when present. Later retail returns or B2B tasks may extend this foundation after policy is defined. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented employee and retail portal gaps and greenfield rebuild sequence into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task excludes undefined fulfillment, returns, refund, and B2B quote behavior. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue and session-wide routine acceptance gates. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
