window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-012"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Inventory greenfield supplier placeholders and source gaps, then record a bounded decision to defer building a supplier portal until an owner defines product scope. Do not create supplier pages, APIs, accounts, workflows, or access-policy changes in this task. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The roles document names <code>employee_supplier</code> as a placeholder liaison role and marks supplier API and middleware surfaces as placeholders, while the roadmap lists the supplier portal as not started (role and middleware placeholder only). The nine-portal handoff does not define a supplier portal section with users, pages, or workflows. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>The greenfield rebuild already reserves supplier in shared access and portal foundations (<code>employee_supplier</code> role, supplier portal ACL map, <code>/supplier</code> proxy matcher, supplier accent label) without shipping supplier portal pages or supplier APIs. No source-confirmed supplier user group, data boundary, or business flow exists to implement. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Produce a source-grounded inventory of supplier placeholders in documentation and in the greenfield <code>app/web</code> access/foundation layers. Record the operator-aligned default decision: <strong>defer</strong> greenfield supplier portal product work until an owner supplies a defined scope covering intended users, tasks, data boundaries, and authorization. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Keep existing reserved ACL and foundation placeholders unchanged. Do not invent supplier liaison workflows, broker-facing supplier disclosure behavior, or a portal shell beyond the current reservation. If later evidence shows an urgent security issue or active supplier path requirement, route that evidence to a separate reviewed task rather than expanding this scope record into portal construction. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Leave non-rebuild <code>TASK-SUPPLIER-001</code> on hold and unchanged. This task is the greenfield rebuild scope record only. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Build a greenfield <code>/supplier</code> portal from the reserved role and accent. This is rejected because the source defines no supplier users, tasks, pages, data model, or business flow. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Remove <code>employee_supplier</code>, the supplier portal ACL entry, the <code>/supplier</code> proxy matcher, and the supplier accent now. This is rejected because the source does not establish a retire decision, and removing reservations without owner authority invents policy. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Recover or port legacy Nuxt/Laravel supplier middleware and login debt. This is rejected under the greenfield-only rebuild decision; legacy <code>TASK-SUPPLIER-001</code> remains on hold separately. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Treat supplier scope as identical to vendor, publisher, or author portals. This is rejected because those portals have documented product surfaces, while supplier remains a placeholder with no portal section in the nine-portal list. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: supplier portal is documented as not started; greenfield code reserves role/ACL/proxy/accent without supplier pages or APIs. Target: a written inventory and defer decision identify current placeholders, state implement-or-retire triggers, and confirm no supplier portal product surface was added by this task. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: no source-confirmed supplier workflow or data boundary exists. Target: inspection proves this task creates no supplier account seed, portal page, supplier API route, role-policy change, redirect change, or workflow, and does not mutate <code>TASK-SUPPLIER-001</code>. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>This task inventories placeholders and records a deferred product decision for the greenfield rebuild. It excludes portal construction and access changes. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Inventory source-confirmed supplier placeholders in roles, roadmap, and the absence of a supplier section among the nine portals. &lt;!-- authority: llm-explicit --&gt;</li><li>Inventory greenfield reservations in <code>access.mjs</code>, <code>web-foundations.mjs</code>, and <code>proxy.ts</code> without treating them as a shipped portal. &lt;!-- authority: llm-explicit --&gt;</li><li>Record the defer decision and the conditions that require a later implement or retire task. &lt;!-- authority: human-confirmed --&gt;</li><li>Preserve a reviewable decision artefact under this task's <code>ship/</code> folder. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Create a supplier portal page, layout, dashboard, API, data model, seed account, notification event, or workflow. &lt;!-- authority: human-confirmed --&gt;</li><li>Change role policy, portal ACL membership, proxy matchers, redirects, login behavior, or foundation accents. &lt;!-- authority: human-confirmed --&gt;</li><li>Retire or expand <code>employee_supplier</code>, recover legacy supplier middleware, deploy, or alter <code>TASK-SUPPLIER-001</code>. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 2 provides the single-role access core that already lists <code>employee_supplier</code> and a supplier portal ACL reservation. Task 3 provides shared portal foundations that already list a supplier accent label. Later implement or retire work requires a separate owner-approved task with source-confirmed users, workflow, data boundary, and authorization scope. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented supplier placeholder gap and greenfield reservations into this scope task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task records a defer decision and does not invent supplier users, pages, APIs, data, or access changes. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue and session-wide routine acceptance gates; non-rebuild on-hold supplier work stays untouched. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-012.</em></p>";
