window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-018"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield <code>/author</code> portal foundation under the TASK-REBUILD-016 owner-acceptance activation gate: signed-session author manuscript-request surfaces that are non-financial, plus an author dashboard that returns explicit policy-pending placeholders for earnings and expanded pipeline stages. Do not invent rates, splits, allocation, settlement math, tax, payouts, ledger values, review-stage machines, or dashboard financial amounts. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The <code>/author</code> portal is documented as needing real manuscript pipeline stages and earnings once a royalty model exists, while manuscript submit and requests list/detail are named as existing author surfaces in the handoff. Author dashboard earnings/stages remain fully mocked and blocked on the royalty/earnings product decision. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Greenfield already reserves the <code>author</code> role, portal ACL, orange accent, and <code>/author</code> proxy matcher. Task 16 recorded a versioned policy proposal whose decision-register rows are unresolved, and Task 17 shipped publisher scaffolding with an executable activation-gate refuse path that must remain intact. No <code>app/web/src/app/api/author/**</code> tree or author earnings core exists. Activating earnings computation, royalty math, or payout automation would invent financial policy the sources forbid. Inventing multi-party review stages would also invent product policy not accepted in the decision register. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite author-portal module and signed-session route handlers in <code>app/web</code> that serve <code>author</code> and <code>admin</code> actors under the existing portal ACL. The module MUST encode the TASK-REBUILD-016 activation gate as an executable refuse path: while any applicable decision-register row remains unresolved (or no owner-accepted activation record is present), financial behavior is refused and financial dashboard fields remain policy-pending placeholders — never invented numbers. Reuse the existing royalty decision-register / activation-gate semantics already shipped by Task 17 (same register areas and acceptance table contract) without rewriting or weakening publisher portal scaffolding, routes, or refuse paths. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Persist author-owned <code>author_manuscript_requests</code> as non-financial manuscript submissions with title, optional notes, opaque private storage key (not a public URL), author id, status limited to <code>submitted</code> / <code>withdrawn</code>, and timestamps. Persist an append-only <code>author_manuscript_request_logs</code> trail that records only those source-confirmed status transitions (<code>submitted</code> on create, <code>withdrawn</code> on withdraw) with actor id and timestamp. Allow the owning author (or admin) to create, list, read detail (including log trail), and withdraw their requests. Do not invent review-stage machines, print-on-demand, royalty linkage, product auto-publish, payment fields, or intermediate lifecycle statuses beyond <code>submitted</code> / <code>withdrawn</code>. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Expose an author dashboard read that returns: (a) non-financial counts derived only from that author's manuscript requests (for example submitted and withdrawn counts); (b) explicit <code>policyPending: true</code> (or equivalent closed marker) for earnings and for expanded pipeline stages beyond the confirmed submitted/withdrawn evidence; (c) an <code>activationGate</code> object naming the unresolved decision-register areas and forbidding calculated amounts. Dashboard responses MUST NOT include earnings amounts, royalty totals, tax figures, payout facts, fabricated zero-as-earnings values presented as live financial facts, or invented review-stage progress scores. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Any API or core function that would compute author earnings, allocate sales to an author, persist earnings ledgers, issue payouts or payment instructions, invoice, or expose a dashboard financial value MUST throw or return a closed policy-pending / activation-blocked error while the gate is open. Emit safe structured events that omit session tokens, email addresses, request bodies, payment secrets, and storage keys. Leave <code>TASK-AUTHOR-001</code>, <code>TASK-ROYALTY-001</code>, <code>docs/royalty/*</code>, and Task 17 publisher portal files intact. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Invent default royalty rates, party splits, or earnings formulas so the dashboard can show real money. This is rejected because Task 16's activation gate requires owner acceptance of every applicable decision-register row before financial activation, and the sources name royalty/earnings as an owner product decision. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Invent a multi-stage manuscript review workflow (for example under-review / accepted / rejected) so the dashboard can show \"real stages.\" This is rejected because no owner-accepted stage machine is documented for greenfield; only submitted/withdrawn evidence plus an append-only status log is source-safe, and expanded stages remain policy-pending. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Reuse vendor payout cores or publisher royalty computation as author earnings. This is rejected because vendor payouts are marketplace seller settlement, Task 16 separates publisher/author earnings from vendor payouts, and Task 17 publisher scaffolding must remain intact rather than be rewritten into a shared settlement engine here. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Mutate or rewrite <code>TASK-AUTHOR-001</code> / <code>TASK-ROYALTY-001</code> artefacts, or reopen Task 17 publisher files to \"share\" financial math. This is rejected under greenfield-only rebuild discipline and standing orders to leave publisher scaffolding/refuse paths and non-rebuild done artefacts unchanged. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Defer the entire author portal until every financial rule is accepted. This is rejected because manuscript submit and requests list/detail are source-named non-financial author surfaces that can ship under an explicit policy-pending dashboard without inventing settlement math or review stages. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: greenfield has no author APIs or author earnings core; earnings/stages remain mocked. Target: tests prove an <code>author</code> can create/list/detail/withdraw own manuscript requests with a submitted/withdrawn log trail, and read a dashboard that returns non-financial counts plus explicit policy-pending markers for earnings and expanded pipeline stages; unauthorized roles and cross-author access fail. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: Task 16 decision-register rows are unresolved; no owner-accepted rates/splits/allocation/tax/payout rules exist. Target: tests prove earnings computation, sales-allocation totals, earnings persistence, payout/payment-instruction paths, and dashboard financial amounts are refused or marked policy-pending while the gate is open; responses and events omit session tokens, emails, request bodies, payment secrets, and storage keys; <code>TASK-AUTHOR-001</code> / <code>TASK-ROYALTY-001</code> / <code>docs/royalty/*</code> / Task 17 publisher portal scaffolding and refuse paths / vendor payout cores are not mutated for ownership. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes the greenfield author portal under the royalty activation gate. It ships non-financial scaffolding and policy-pending financial/stage placeholders only. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Signed-session author/admin manuscript-request create, list, detail (with submitted/withdrawn log trail), and withdraw. &lt;!-- authority: human-confirmed --&gt;</li><li>Author dashboard with non-financial counts and explicit policy-pending placeholders for earnings and expanded pipeline stages. &lt;!-- authority: human-confirmed --&gt;</li><li>Executable activation-gate refuse path that blocks earnings computation, sales allocation to authors, earnings ledger writes, payouts, payment instructions, invoices, and dashboard financial values while decision-register acceptance is absent. &lt;!-- authority: human-confirmed --&gt;</li><li>Reuse of Task 16/17 royalty decision-register activation-gate semantics without rewriting publisher portal scaffolding. &lt;!-- authority: human-confirmed --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, payment secrets, and storage keys. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Choose or hard-code royalty rates, party splits, eligibility formulas, recoupment, tax/withholding, reporting calendars, currency conversion, or payout controls. &lt;!-- authority: human-confirmed --&gt;</li><li>Compute author earnings, allocate sales to authors, persist financial ledgers, issue payouts or payment instructions, invoices, or display live dashboard financial amounts. &lt;!-- authority: human-confirmed --&gt;</li><li>Invent multi-party review-stage machines, print-on-demand, product auto-publish from manuscripts, or publisher royalty surfaces (Task 17 owns those). &lt;!-- authority: llm-explicit --&gt;</li><li>Mutate <code>TASK-AUTHOR-001</code>, <code>TASK-ROYALTY-001</code>, <code>docs/royalty/*</code>, Task 17 publisher portal scaffolding/refuse paths, vendor payout cores, or deploy. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 2 provides identity, roles, and portal ACL for <code>author</code> / <code>admin</code>. Task 16 provides the royalty/earnings policy proposal and owner-acceptance activation gate that this task must enforce before any financial behavior. Task 17 already ships publisher scaffolding and the shared activation-gate refuse pattern; this task must consume that gate semantics without mutating those files' ownership or weakening refuse paths. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented author portal needs and TASK-REBUILD-016 activation gate into this greenfield rebuild task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task builds non-financial author scaffolding and policy-pending dashboard placeholders; it excludes invented rates, splits, settlement math, ledgers, payouts, review-stage machines, and live financial dashboard values. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue, session-wide routine acceptance gates, and the standing order that financial activation requires accepted decision-register rows. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-018.</em></p>";
