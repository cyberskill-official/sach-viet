window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-017"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield <code>/publisher</code> portal foundation under the TASK-REBUILD-016 owner-acceptance activation gate: signed-session publisher publishing-request and MARC-metadata surfaces that are non-financial, plus a publisher dashboard that returns explicit policy-pending placeholders for royalties, sales rollups, and contracts. Do not invent rates, splits, allocation, settlement math, tax, payouts, ledger values, or dashboard financial amounts. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The <code>/publisher</code> portal is documented as needing a royalty model plus real sales rollups and contract management, while product list/submit and MARC upload are named as existing publisher surfaces in the handoff. Publisher dashboard royalties/sales/contracts remain fully mocked and blocked on the royalty/earnings product decision. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Greenfield already reserves the <code>publisher</code> role, portal ACL, purple accent, and <code>/publisher</code> proxy matcher, and Task 16 recorded a versioned policy proposal whose decision-register rows are unresolved. No <code>app/web/src/app/api/publisher/**</code> tree or publisher royalty core exists. Activating royalty computation, sales allocation, or payout automation would invent financial policy the sources forbid. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite publisher-portal module and signed-session route handlers in <code>app/web</code> that serve <code>publisher</code> and <code>admin</code> actors under the existing portal ACL. The module MUST encode the TASK-REBUILD-016 activation gate as an executable refuse path: while any applicable decision-register row remains unresolved (or no owner-accepted activation record is present for this task), financial behavior is refused and financial dashboard fields remain policy-pending placeholders — never invented numbers. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Persist publisher-owned <code>publishing_requests</code> as non-financial catalog/manuscript submissions with title, optional notes, opaque private storage key (not a public URL), submitter id, status limited to <code>submitted</code> / <code>withdrawn</code>, and timestamps. Allow the owning publisher (or admin) to create, list, and withdraw their requests. Do not invent review-stage machines, print-on-demand, royalty linkage, product auto-publish, or payment fields on these rows. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Persist publisher-owned private <code>publisher_marc_records</code> metadata keyed by <code>(publisher_id, product_id)</code> with an opaque storage key (not a public URL). Allow the owning publisher (or admin) to register or replace and list their MARC metadata for catalog product ids that already exist. Do not invent a MARC serialization format, public download URL, institution entitlement change, or royalty attribution from MARC rows. Institution MARC tables and handlers from Task 15 MUST remain intact. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Expose a publisher dashboard read that returns: (a) non-financial counts derived only from that publisher's publishing requests and MARC rows; (b) explicit <code>policyPending: true</code> (or equivalent closed marker) for royalties, sales rollups, and contracts; (c) an <code>activationGate</code> object naming the unresolved decision-register areas and forbidding calculated amounts. Dashboard responses MUST NOT include royalty amounts, sales USD totals, contract values, payout facts, tax figures, or fabricated zero-as-earnings values presented as live financial facts. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Any API or core function that would compute royalties, allocate sales to a publisher, persist earnings ledgers, issue payouts or payment instructions, invoice, or expose a dashboard financial value MUST throw or return a closed policy-pending / activation-blocked error while the gate is open. Emit safe structured events that omit session tokens, email addresses, request bodies, payment secrets, and storage keys. Leave <code>TASK-PUBLISHER-001</code>, <code>TASK-ROYALTY-001</code>, and <code>docs/royalty/*</code> unchanged. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Invent default royalty rates, party splits, or sales-allocation rules so the dashboard can show real money. This is rejected because Task 16's activation gate requires owner acceptance of every applicable decision-register row before financial activation, and the sources name royalty/earnings as an owner product decision. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Reuse vendor payout cores as publisher royalties. This is rejected because vendor payouts are marketplace seller settlement; Task 16 explicitly separates them from publisher/author earnings. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Un-hold or mutate <code>TASK-PUBLISHER-001</code> / rewrite <code>TASK-ROYALTY-001</code> artefacts. This is rejected under greenfield-only rebuild discipline and standing orders to leave non-rebuild on_hold work alone. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Defer the entire publisher portal until every financial rule is accepted. This is rejected because product list/submit and MARC upload are source-named non-financial publisher surfaces that can ship under an explicit policy-pending dashboard without inventing settlement math. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: greenfield has no publisher APIs or publisher core; royalties/sales/contracts remain mocked. Target: tests prove a <code>publisher</code> can create/list/withdraw own publishing requests, register/list own private MARC metadata for existing catalog products, and read a dashboard that returns non-financial counts plus explicit policy-pending markers for royalties, sales, and contracts; unauthorized roles and cross-publisher access fail. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: Task 16 decision-register rows are unresolved; no owner-accepted rates/splits/allocation/tax/payout rules exist. Target: tests prove royalty computation, sales-allocation totals, earnings persistence, payout/payment-instruction paths, and dashboard financial amounts are refused or marked policy-pending while the gate is open; responses and events omit session tokens, emails, request bodies, payment secrets, and storage keys; <code>TASK-PUBLISHER-001</code> / <code>TASK-ROYALTY-001</code> / <code>docs/royalty/*</code> / vendor payout and institution MARC cores are not mutated for ownership. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes the greenfield publisher portal under the royalty activation gate. It ships non-financial scaffolding and policy-pending financial placeholders only. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Signed-session publisher/admin publishing-request create, list, and withdraw. &lt;!-- authority: human-confirmed --&gt;</li><li>Signed-session publisher/admin private MARC metadata register/replace and list for existing catalog products, without changing institution MARC entitlement. &lt;!-- authority: human-confirmed --&gt;</li><li>Publisher dashboard with non-financial counts and explicit policy-pending placeholders for royalties, sales rollups, and contracts. &lt;!-- authority: human-confirmed --&gt;</li><li>Executable activation-gate refuse path that blocks royalty computation, sales allocation, earnings ledger writes, payouts, payment instructions, invoices, and dashboard financial values while decision-register acceptance is absent. &lt;!-- authority: human-confirmed --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, payment secrets, and storage keys. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Choose or hard-code royalty rates, party splits, eligibility formulas, recoupment, tax/withholding, reporting calendars, currency conversion, or payout controls. &lt;!-- authority: human-confirmed --&gt;</li><li>Compute royalties, allocate sales to publishers, persist financial ledgers, issue payouts or payment instructions, invoices, or display live dashboard financial amounts. &lt;!-- authority: human-confirmed --&gt;</li><li>Invent contract-management financial workflows, product auto-publish from requests, print-on-demand, or author-earnings surfaces (Task 18). &lt;!-- authority: llm-explicit --&gt;</li><li>Mutate <code>TASK-PUBLISHER-001</code>, <code>TASK-ROYALTY-001</code>, <code>docs/royalty/*</code>, vendor payout cores, institution MARC cores, or deploy. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 4 provides catalog products that publisher MARC metadata references by product id. Task 16 provides the royalty/earnings policy proposal and owner-acceptance activation gate that this task must enforce before any financial behavior. Task 18 may later build author earnings under the same gate and must not be unblocked by inventing shared settlement math here. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented publisher portal needs and TASK-REBUILD-016 activation gate into this greenfield rebuild task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task builds non-financial publisher scaffolding and policy-pending dashboard placeholders; it excludes invented rates, splits, settlement math, ledgers, payouts, and live financial dashboard values. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue, session-wide routine acceptance gates, and the standing order that financial activation requires accepted decision-register rows. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-017.</em></p>";
