window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-API-001"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>New and changed hot-path handlers use <code>{ error: { code, message, requestId } }</code> and list pages use <code>{ items, nextCursor }</code>. Do not big-bang every legacy <code>{ error: string }</code> in one PR (<code>PKG-11</code> start). &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p><code>PKG-11</code> requires a shared error envelope and cursor lists so clients can recover deterministically (<code>FL-B2C-02</code>, <code>FL-PLT-02</code>). Current handlers still mix string errors and ad-hoc lists. The golive wave asks for an incremental contract on hot paths only, not a full rewrite of every legacy response. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Apply the envelope to new/changed handlers on hot paths: auth, catalog/search, checkout, orders, support, vendor offers, and other handlers this wave already touches. List endpoints on those paths return <code>{ items, nextCursor }</code> (null cursor means end). Include <code>requestId</code> on errors. Leave untouched legacy <code>{ error: string }</code> handlers alone unless this wave changes them. Tests import handlers and assert JSON shape; do not add source-grep route tests. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: no new authz model; existing role guards stay. Contract applies to the hot paths named above when they are new or changed. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Rewrite every legacy <code>{ error: string }</code> in one PR. Rejected — golive wave says incremental, not big-bang. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Invent tax/shipping/royalty fields in the envelope. Rejected — DEC records unsigned; envelope is transport only. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: mixed string errors and non-cursor lists on hot paths. Target: new/changed hot-path handlers emit the named envelope and cursor page shape; <code>npm test</code> asserts them. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: no Production deploy or merge from this task; no live keys; do not break untouched legacy clients by rewriting every route. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li><code>{ error: { code, message, requestId } }</code> on new/changed hot-path handlers. &lt;!-- authority: llm-explicit --&gt;</li><li><code>{ items, nextCursor }</code> on new/changed hot-path lists. &lt;!-- authority: llm-explicit --&gt;</li><li>Tests: handler-imported JSON shape assertions on those paths (extend existing <code>tests/*-http.test.mjs</code>; no new <code>*-route.test.mjs</code> greps). &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Full PKG-11 typed client, every legacy route, finance math, live keys, WordPress DNS, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-TEST-001</code> and <code>TASK-DATA-001</code> are the in-flight audit set (HTTP suites + async pg). This task is eligible only after those complete HITL (<code>testing → done</code>). HITL remains required at reviewing → ready_to_test and testing → done. Do not request HITL while authoring. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor agent authoring the local-complete golive wave queue. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates after implementation. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
