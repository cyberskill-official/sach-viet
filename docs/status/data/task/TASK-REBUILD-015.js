window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-015"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield institution buyer portal capabilities in <code>app/web</code> on top of the existing blind quote and order reads: an informational organization budget, institution-initiated purchase-order submission against organization-owned <code>awaiting_po</code> B2B orders, and entitlement-gated private MARC delivery for titles on confirmed organization orders — without rewriting the B2B quote/order cores or disclosing upstream suppliers. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>The <code>/institution</code> portal already has catalog, quotes, and selection lists, but its dashboard budget and approvals area is display-only. The portal docs call out the remaining needs: real budget tracking, PO submission, and MARC record delivery for purchased titles. The vision defines blind brokerage where institutions request quotes and order via PO, with MARC support for library cataloging. Tasks 13 and 14 deliberately deferred institution budget, institution-initiated PO submission, and MARC delivery. The greenfield app has organization-scoped blind quote/order reads and staff-attached contract/PO artifacts, but no institution buyer write path for budgets, PO submission, or MARC entitlement. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite institution-buyer module and signed-session route handlers in <code>app/web</code> that compose organization membership, catalog product ids, and existing B2B order records without rewriting <code>b2b-quote-core</code> or <code>b2b-order-core</code> ownership, status machines, or existing institution blind-read handlers. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Persist one informational <code>institution_budgets</code> row per organization with a non-negative USD decimal string using the same money conventions as commerce, plus <code>updated_by</code> and <code>updated_at</code>. Allow <code>school_librarian</code> and <code>admin</code> actors to read the budget for the actor's organization (admin may read any organization when an organization id is supplied) and to upsert the informational amount. The budget MUST NOT reserve, charge, approve, reject, or otherwise change financial or order state. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Allow <code>school_librarian</code> and <code>admin</code> actors to submit a purchase-order artifact against an organization-owned B2B order that is in <code>awaiting_po</code> status by supplying a staff-facing reference number and an opaque private storage key that is not a public URL. The submission MUST create a <code>purchase_order</code> artifact on that order using the existing artifact kinds and leave order status unchanged for staff confirmation. Institution responses after submission MUST return artifact reference numbers and omit storage keys, vendor ids, supplier identifiers, payout facts, and Stripe fields. Existing institution order/quote blind-read routes MUST remain behaviorally intact. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Persist private <code>institution_marc_records</code> metadata keyed by <code>product_id</code> with an opaque storage key (not a public URL). Allow <code>employee_b2b</code> and <code>admin</code> actors to register or replace MARC metadata for a catalog product. Allow <code>school_librarian</code> and <code>admin</code> actors to list and read MARC metadata only for product ids that appear on at least one <code>confirmed</code> B2B order owned by the actor's organization. Institution MARC responses MUST omit storage keys from list payloads unless the detail read is entitlement-gated, and even then MUST NOT invent a new MARC serialization format, public download URL, email delivery, or third-party catalog push — return only the private metadata fields needed for an entitled private fetch (product id, opaque storage key, updated_at). Emit safe structured events that omit session tokens, email addresses, request bodies, payment secrets, and storage keys. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Rewrite quote or order cores to own institution budgets and MARC tables. This is rejected because Tasks 13 and 14 already own those domains, and standing rebuild discipline requires leaving quote/order cores and institution blind reads intact while building the buyer portal on top. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Treat the budget as a ledger that reserves, commits, or charges against PO submission. This is rejected because the sources say budget/PO tracking is not designed as a financial system, and the approved institution handoff limited budget to informational use. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Auto-confirm a B2B order when the institution submits a PO. This is rejected because Task 14 already defines staff-gated confirmation after a purchase-order artifact exists; institution submission supplies the artifact, staff still confirm. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Generate a new MARC download format, public URL, or third-party catalog delivery. This is rejected because the sources establish MARC as a library need and publisher upload concept, but do not establish a delivery serialization, public access, or external integration. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Recover a legacy Laravel/Nuxt institution budget or MARC delivery implementation. This is rejected under the greenfield-only rebuild decision. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: institution budget is display-only, institutions cannot submit POs, and no entitlement-gated MARC delivery exists. Target: tests prove a <code>school_librarian</code> can upsert and read an informational organization budget; submit a purchase-order artifact against an organization-owned <code>awaiting_po</code> order without changing order status; and list/read private MARC metadata only for products on that organization's <code>confirmed</code> orders; unauthorized roles and cross-organization access fail. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: brokerage is blind; quote and order cores plus institution blind reads must stay intact; budget must not become a financial engine. Target: tests prove institution responses omit vendor/supplier identifiers and storage keys except the entitlement-gated MARC detail storage key; budget upsert does not mutate orders or quotes; PO submission does not confirm or cancel orders; <code>b2b-quote-core</code> and existing institution blind-read handlers are not rewritten for ownership; responses and structured events omit session tokens, email addresses, request bodies, payment secrets, and non-entitled storage keys. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes greenfield institution buyer budget, PO submission, and entitlement-gated private MARC delivery under signed sessions on top of Tasks 4, 13, and 14. It does not invent financial ledgers, approval engines, MARC serializers, public document hosting, or deployment. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Informational per-organization budget upsert and read for <code>school_librarian</code> / <code>admin</code>. &lt;!-- authority: human-confirmed --&gt;</li><li>Institution-initiated <code>purchase_order</code> artifact submission against organization-owned <code>awaiting_po</code> B2B orders without changing order status. &lt;!-- authority: human-confirmed --&gt;</li><li>Private product-keyed MARC metadata registration by <code>employee_b2b</code> / <code>admin</code>, and entitlement-gated institution list/detail for products on confirmed organization orders. &lt;!-- authority: human-confirmed --&gt;</li><li>Preserve existing institution quote/order blind-read behavior and organization isolation. &lt;!-- authority: human-confirmed --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, payment secrets, and non-entitled storage keys. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Rewriting <code>b2b-quote-core</code>, <code>b2b-order-core</code> status machines, staff conversion/attach/transition APIs, or existing institution blind-read handlers. &lt;!-- authority: human-confirmed --&gt;</li><li>Budget reservation, charging, reconciliation, auto-approve/reject, or any financial ledger. &lt;!-- authority: human-confirmed --&gt;</li><li>Auto-confirming or cancelling B2B orders from institution PO submission. &lt;!-- authority: llm-explicit --&gt;</li><li>New MARC serialization formats, public URLs, email/Zalo delivery, third-party catalog integrations, publisher royalty work, or deployment. &lt;!-- authority: human-confirmed --&gt;</li><li>Legacy WordPress/Laravel/Nuxt recovery or changing non-rebuild <code>on_hold</code> tasks. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 4 provides catalog products that MARC metadata and purchased titles reference. Task 13 provides organizations, membership, selection lists, and institution quote reads that remain intact. Task 14 provides organization-owned B2B orders, <code>awaiting_po</code>/<code>confirmed</code> statuses, and <code>purchase_order</code> artifact kinds that institution PO submission and MARC entitlement compose. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented institution portal, B2B brokerage, and data-model sources into this greenfield rebuild task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task builds informational budgets, institution PO submission, and entitlement-gated private MARC delivery and excludes financial ledgers, auto-confirmation, MARC serializers, public hosting, quote/order core rewrites, and legacy recovery. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue and session-wide routine acceptance gates. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<p><em>End of TASK-REBUILD-015.</em></p>";
