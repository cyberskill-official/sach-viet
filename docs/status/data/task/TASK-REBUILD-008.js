window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REBUILD-008"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Build the greenfield vendor portal foundation beyond offer writes: vendor-scoped incoming order reads, admin-managed payout records with payout items linked to order items, vendor payout history, and a vendor dashboard summary derived from those existing records. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Problem</h2>\n<p>Approved vendors can already write offers and submit applications, and administrators can approve applications, but sellers still lack a signed-session view of their incoming orders and payout history. The source identifies vendor incoming orders as read-only, payouts history, marketplace <code>Payout</code>/<code>PayoutItem</code> settlement records as admin-managed, and vendor dashboard KPIs that must not remain mocked for the baseline facts already available from orders and payouts. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Add a SQLite vendor-commerce repository and signed-session route handlers in <code>app/web</code>. Vendors and administrators may list order rows that contain that vendor's offer line items without exposing unrelated sellers' lines or customer secrets. Administrators create payout records with an explicit USD amount and one or more payout items that reference that vendor's order items; vendors may only read their own payouts. A vendor dashboard summary exposes counts and paid-line USD totals derived from those scoped order and payout rows. Emit safe structured events for payout creation. &lt;!-- authority: human-confirmed --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Invent settlement eligibility, cadence, commission formulas, or automatic money transfer. This is rejected because the source records payouts as admin-managed and does not define a settlement formula; Task 7 already deferred that policy. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Add order fulfillment transitions such as mark-shipped with tracking. This is deferred because the source identifies fulfillment actions but does not define the greenfield fulfillment-state contract, and Task 7 already excluded those transitions. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Add report export or chart-series analytics beyond the summary facts above. This is deferred to later vendor analytics work once the baseline order and payout reads exist. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: the greenfield application has no vendor-scoped order or payout history surface. Target: tests prove a vendor can read only their incoming order lines and payout history, an administrator can create an admin-managed payout for that vendor's order items, and a non-vendor cannot read another vendor's payouts. Deadline: before this task moves from testing to done. &lt;!-- authority: human-confirmed --&gt;</p>\n<p>Guardrail - baseline: payout amounts and settlement rules are undefined beyond admin-managed records, and customer secrets must stay out of seller views. Target: tests prove payout creation requires an administrator-supplied USD amount, rejects foreign order items, creates no transfer or settlement formula, and omits customer email, session tokens, and payment secrets from vendor responses and events. Deadline: before this task moves from testing to done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<p>This task establishes vendor-scoped order reads, admin-managed payout persistence, vendor payout history, and a summary dashboard derived from those records. It does not invent settlement policy, money movement, fulfillment transitions, report export, catalog import, or deployment. &lt;!-- authority: llm-explicit --&gt;</p>\n<h3>In scope</h3>\n<ul><li>Vendor-scoped incoming order reads for line items tied to that vendor's offers, including administrator override reads. &lt;!-- authority: human-confirmed --&gt;</li><li>Admin-managed <code>payouts</code> and <code>payout_items</code> persistence with an explicit USD amount and links to eligible order items. &lt;!-- authority: human-confirmed --&gt;</li><li>Vendor payout history reads limited to the authenticated vendor (or administrator acting for a vendor). &lt;!-- authority: human-confirmed --&gt;</li><li>Vendor dashboard summary totals derived from the vendor's order lines and payout records. &lt;!-- authority: llm-explicit --&gt;</li><li>Safe structured events that exclude session tokens, email addresses, request bodies, and payment secrets. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Settlement eligibility rules, commission formulas, cadence, bank transfer, tax, invoice, or ledger accounting. &lt;!-- authority: human-confirmed --&gt;</li><li>Order fulfillment states, shipment tracking, returns, refunds, or customer-service actions. &lt;!-- authority: llm-explicit --&gt;</li><li>Report CSV/PDF export, chart-series analytics beyond the summary totals, or legacy WordPress/Laravel/Nuxt recovery. &lt;!-- authority: human-confirmed --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p>Task 4 provides vendor offers and ownership checks. Task 5 provides orders and paid USD line items. Task 7 provides vendor role assignment through the admin application queue. Later analytics or report-export tasks may extend the vendor dashboard after these baseline reads exist. &lt;!-- authority: llm-implicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Codex translated the documented vendor portal, marketplace payout records, and greenfield rebuild sequence into this task. &lt;!-- authority: human-confirmed --&gt;</li><li>Scope: The task excludes undefined settlement formulas, money transfer, and fulfillment transitions. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: The operator approved the greenfield-only queue and session-wide routine acceptance gates. &lt;!-- authority: human-confirmed --&gt;</li></ul>";
