window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-UI-002"]="<h1>Task</h1>\n<h2>Summary</h2>\n<p>Deepen existing APIs into walkable pages: customer account + stored-but-unused addresses + order timeline; support assignment; vendor offer create/edit; packing/shipped/delivered as operational notes only. Extend local seed; refuse <code>NODE_ENV=production</code> and non-loopback/<code>db</code> <code>DATABASE_URL</code>. No new finance math. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Problem</h2>\n<p>TASK-UI-001 wires portals to existing APIs. The golive wave still needs a local nine-portal walkthrough: customer account (<code>FL-ID-07</code>), support assignment, vendor offer editor (<code>FL-VEN-03</code>), and a minimal fulfillment overlay. Legal shipping contracts, tax, commissions, and royalties stay unsigned. Publisher/author finance stays the existing policy notice. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Proposed Solution</h2>\n<p>Reuse existing cores: <code>support-core.mjs</code>, <code>vendor-commerce-core.mjs</code>, <code>catalog-core.mjs</code>, <code>employee-retail-core.mjs</code>, <code>b2b-quote-core.mjs</code>, <code>storage-core.mjs</code>, <code>publisher-portal-core.mjs</code>, <code>admin-dashboard.tsx</code>. Customer: email, password change, locale; addresses stored but unused in quote; order timeline. Vendor: offer create/edit; incoming lines; mark <code>packing | shipped | delivered</code> on own lines (status column or side table) as operational notes, not legal shipping. Admin: split catalog / applications / payouts / flags; failed panels must error, not show zero. Employee/retail: <code>assignee_id</code> on tickets; paid-order list + fulfillment overlay. B2B/institution: pipeline, quote detail, convert, PO upload via <code>stored_objects</code>. Publisher/author: requests/withdraw/MARC only. Extend <code>seed-local-core.mjs</code> so <code>npm run seed:local</code> against Compose Postgres walks every current role; already refuses <code>NODE_ENV=production</code>; fingerprint must also refuse if <code>DATABASE_URL</code> host is not loopback/<code>db</code>. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Routes and authz: existing portal matchers; customer account/settings; vendor offer write stays vendor/admin; support assignment is employee/admin; vendor cannot write another vendor's offers. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Alternatives Considered</h2>\n<p>Invent tax, shipping rates, or treat packing/shipped/delivered as a legal carrier contract. Rejected — DEC-COM-001 / DEC-RET-001 unsigned; overlay is operational notes only. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Build publisher/author finance ledgers. Rejected — DEC-ROY-001 / DEC-SET-001; keep the policy notice. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Reopen TASK-REBUILD-001…023. Rejected — those stay done. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - baseline: portals exist but account, assignment, offer editor, and fulfillment overlay are incomplete. Target: each named portal action works against real APIs locally; seed walks current roles; failed admin panels error not zero; <code>npm test</code> covers named cores. Deadline: before reviewing → ready_to_test. &lt;!-- authority: llm-explicit --&gt;</p>\n<p>Guardrail - baseline: Production may already be live. Target: no Production deploy, merge, or <code>seed:local</code> against Production; no <code>sk_live_</code> / <code>PAYPAL_MODE=live</code>; no invented finance numbers. Deadline: ongoing. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Customer account (email, password change, locale), addresses stored but unused in quote, order timeline. &lt;!-- authority: llm-explicit --&gt;</li><li>Support ticket <code>assignee_id</code> and employee/retail paid-order fulfillment overlay. &lt;!-- authority: llm-explicit --&gt;</li><li>Vendor offer create/edit and own-line <code>packing | shipped | delivered</code> notes. &lt;!-- authority: llm-explicit --&gt;</li><li>Admin panel split; failed panels error, not zero. &lt;!-- authority: llm-explicit --&gt;</li><li>B2B quote detail/convert/PO via <code>stored_objects</code>; publisher/author requests/MARC only. &lt;!-- authority: llm-explicit --&gt;</li><li>Seed fingerprint: refuse production env and non-loopback/<code>db</code> hosts. &lt;!-- authority: llm-explicit --&gt;</li><li>Tests: extend <code>tests/seed-local-core.test.mjs</code>, support/vendor/catalog core tests (handler/core, not greps). &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h3>Out of scope</h3>\n<ul><li>Tax/shipping/returns/settlement/royalty/B2B payment terms (unsigned DEC-*), live keys, WordPress DNS, Google/MFA, private <code>app</code> schema, self-setting <code>done</code>. &lt;!-- authority: llm-explicit --&gt;</li></ul>\n<h2>Dependencies</h2>\n<p><code>TASK-UI-001</code> and <code>TASK-COM-001</code> are the in-flight audit set. This task is eligible only after those complete HITL (<code>testing → done</code>). HITL remains required at reviewing → ready_to_test and testing → done. Do not request HITL while authoring. &lt;!-- authority: llm-explicit --&gt;</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Cursor agent authoring the local-complete golive wave queue. &lt;!-- authority: llm-explicit --&gt;</li><li>Human review: Operator HITL at the two acceptance gates after implementation. &lt;!-- authority: llm-explicit --&gt;</li></ul>";
