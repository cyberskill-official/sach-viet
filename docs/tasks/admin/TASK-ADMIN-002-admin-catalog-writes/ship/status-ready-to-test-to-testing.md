# Status: ready_to_test → testing

Tester claim after review acceptance. Focused admin-catalog verifies re-run in this session.

Final acceptance HITL (`testing → done`) still required; agent must not self-set `done`.
