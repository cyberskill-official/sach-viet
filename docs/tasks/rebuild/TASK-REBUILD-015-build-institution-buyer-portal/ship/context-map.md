# Context map

Touched domain: institution buyer portal on top of B2B quote/order cores in `app/web`.
Key files: `institution-buyer-core.mjs`, institution budget/PO/MARC routes, B2B MARC registration route, tests + verify script.
Preserved: `b2b-quote-core.mjs`, `b2b-order-core.mjs`, existing institution blind-read handlers.
