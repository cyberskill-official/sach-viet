# Final acceptance

The operator gave a session-wide approval for routine final-acceptance gates. This approval accepts the tested catalog and marketplace core. It does not authorize deployment, push, merge, real credentials, data import, or a change to the approved greenfield scope.
