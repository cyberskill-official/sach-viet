# Out-of-band gates

The AWH and CAF gates are disabled in `.cyberos/gates.env` because this repository has no sealed AWH goldenset or CAF baseline. The configured build, lint, test, coverage, verification, dependency audit, Compose configuration, and whitespace checks passed.
