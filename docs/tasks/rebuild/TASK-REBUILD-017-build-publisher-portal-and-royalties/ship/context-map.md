# Context map

Touched domain: greenfield publisher portal under the TASK-REBUILD-016 royalty activation gate in `app/web`.
Key files: `publisher-portal-core.mjs`, `/api/publisher/dashboard`, `/api/publisher/publishing-requests`, `/api/publisher/marc`, tests + verify script.
Preserved: vendor payout cores, institution MARC cores, `TASK-PUBLISHER-001`, `TASK-ROYALTY-001`, `docs/royalty/*`.
