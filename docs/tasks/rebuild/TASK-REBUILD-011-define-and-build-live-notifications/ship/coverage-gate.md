# Coverage gate

Touched live-notification files covered by unit and route tests for auth, ownership, cursor resume, live publish, secret omission, and verifier presence. Suite: `npm --prefix app/web test` and `npm --prefix app/web run test:coverage`.
