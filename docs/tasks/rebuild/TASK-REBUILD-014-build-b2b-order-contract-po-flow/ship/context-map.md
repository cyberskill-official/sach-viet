# Context map

Touches `app/web` B2B order domain on top of quote + commerce foundations: `b2b-order-core.mjs`, staff `/api/b2b/orders` routes, institution `/api/institution/orders` routes, tests, and `verify-b2b-order-core.mjs`. Leaves `b2b-quote-core.mjs` and B2C Stripe checkout ownership intact.
