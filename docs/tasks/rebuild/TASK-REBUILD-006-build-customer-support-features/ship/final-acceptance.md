# Final acceptance

The operator's session-wide approval covers this routine final-acceptance gate. It accepts the customer support, goods-request, and verified-purchase review implementation after all machine gates pass. It does not authorize deployment, push, merge, credentials, or external notification delivery.
