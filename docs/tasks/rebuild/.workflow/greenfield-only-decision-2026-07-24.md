# Greenfield-only decision

The operator confirmed that legacy-related work must not be implemented because the legacy code is unavailable. Incomplete non-rebuild tasks are placed on hold as a reversible queue decision. Completed legacy tasks remain unchanged as historical evidence. The active delivery sequence is the greenfield Next.js rebuild plan. <!-- authority: human-confirmed -->
