# Payment default

The operator instructed the project to use recommended defaults. For Task 5, that means Stripe hosted Checkout in test mode only. No Stripe secret, publishable key, webhook secret, or live payment action is authorized. PayPal remains a later extension task.
