# Repo context map

The commerce core is contained in `app/web`. It uses the Task 2 signed-session boundary, the Task 4 SQLite catalog and offer records, and the existing Node test and verification tooling. No external payment SDK, legacy code, or provider credential is added.
